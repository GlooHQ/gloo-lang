/*************************************************************************************************

Welcome to Baml! To use this generated code, please run one of the following:

$ npm install @boundaryml/baml
$ yarn add @boundaryml/baml
$ pnpm add @boundaryml/baml

*************************************************************************************************/

// This file was generated by BAML: do not edit it. Instead, edit the BAML
// files and re-generate this code.
//
/* eslint-disable */
// tslint:disable
// @ts-nocheck
// biome-ignore format: autogenerated code
'use client'

import { useCallback, useMemo, useReducer, useTransition } from 'react';
import type {
  StreamingProps,
  NonStreamingProps,
  StreamingHookResult,
  NonStreamingHookResult,
  HookProps,
  HookResult
} from './types';
import type { RecursivePartialNull, Check, Checked } from '../types';
import type { Image, Audio } from "@boundaryml/baml"
import { BamlValidationError, BamlClientFinishReasonError } from "@boundaryml/baml/errors"
import * as ServerActions from './server';

import type {AnotherObject, BigNumbers, BinaryNode, Blah, BlockConstraint, BlockConstraintForParam, BookOrder, ClassForNullLiteral, ClassOptionalOutput, ClassOptionalOutput2, ClassToRecAlias, ClassWithBlockDone, ClassWithImage, ClassWithoutDone, ComplexMemoryObject, CompoundBigNumbers, ContactInfo, CustomTaskResult, DummyOutput, DynInputOutput, DynamicClassOne, DynamicClassTwo, DynamicOutput, Earthling, Education, Email, EmailAddress, Event, FakeImage, FlightConfirmation, FooAny, Forest, FormatterTest0, FormatterTest1, FormatterTest2, FormatterTest3, GroceryReceipt, InnerClass, InnerClass2, InputClass, InputClassNested, LinkedList, LinkedListAliasNode, LiteralClassHello, LiteralClassOne, LiteralClassTwo, MalformedConstraints, MalformedConstraints2, Martian, MemoryObject, MergeAttrs, NamedArgsSingleClass, Nested, Nested2, NestedBlockConstraint, NestedBlockConstraintForParam, Node, NodeWithAliasIndirection, OptionalListAndMap, OptionalTest_Prop1, OptionalTest_ReturnType, OrderInfo, OriginalA, OriginalB, Person, PhoneNumber, Quantity, RaysData, ReceiptInfo, ReceiptItem, Recipe, RecursiveAliasDependency, Resume, Schema, SearchParams, SemanticContainer, SmallThing, SomeClassNestedDynamic, StringToClassEntry, TestClassAlias, TestClassNested, TestClassWithEnum, TestMemoryOutput, TestOutputClass, Tree, TwoStoriesOneTitle, UnionTest_ReturnType, UniverseQuestion, UniverseQuestionInput, WithReasoning, AliasedEnum, Category, Category2, Category3, Color, DataType, DynEnumOne, DynEnumTwo, EnumInClass, EnumOutput, Hobby, MapKey, NamedArgsSingleEnum, NamedArgsSingleEnumList, OptionalTest_CategoryType, OrderStatus, Tag, TestEnum} from "../types"
import * as partial_types from "../types"

/**
 * Type guard to check if props are for streaming mode
 */
function isStreamingProps<Action>(
  props: HookProps<Action>
): props is StreamingProps<Action> {
  return props.stream === true;
}

interface HookState<TPartial, TFinal> {
  isSuccess: boolean;
  error: Error | BamlValidationError | BamlClientFinishReasonError | null;
  data: TFinal | null;
  partialData: TPartial | null;
}

type HookStateAction<TPartial, TFinal> =
  | { type: 'START_REQUEST' }
  | { type: 'SET_ERROR'; payload: Error | BamlValidationError | BamlClientFinishReasonError }
  | { type: 'SET_PARTIAL'; payload: TPartial }
  | { type: 'SET_FINAL'; payload: TFinal }
  | { type: 'RESET' };

function hookReducer<TPartial, TFinal>(
  state: HookState<TPartial, TFinal>,
  action: HookStateAction<TPartial, TFinal>
): HookState<TPartial, TFinal> {
  switch (action.type) {
    case 'START_REQUEST':
      return {
        ...state,
        isSuccess: false,
        error: null,
        data: null,
        partialData: null,
      };
    case 'SET_ERROR':
      return {
        ...state,
        isSuccess: false,
        error: action.payload,
        data: null,
        partialData: null,
      };
    case 'SET_PARTIAL':
      return {
        ...state,
        partialData: action.payload,
      };
    case 'SET_FINAL':
      return {
        ...state,
        isSuccess: true,
        data: action.payload,
        partialData: null,
      };
    case 'RESET':
      return {
        isSuccess: false,
        error: null,
        data: null,
        partialData: null,
      };
    default:
      return state;
  }
}

/**
 * Base hook for making BAML function calls with support for both streaming and non-streaming modes.
 * Provides a unified interface for handling loading states, errors, and data updates.
 *
 * This hook can be used directly with any BAML server action, or through the specialized hooks
 * generated for each BAML function.
 *
 * Features:
 * 1. Streaming Support
 *    - Real-time partial updates via `partialData`
 *    - Progress indicators and incremental UI updates
 *    - Automatic stream cleanup and error handling
 *
 * 2. State Management
 *    - Loading state via `isPending`
 *    - Success/error states
 *    - Final result in `data`
 *    - Partial results in `partialData` (streaming mode)
 *
 * 3. Error Handling
 *    - Type-safe error handling with three possible error types:
 *      1. BamlValidationError: Thrown when BAML fails to parse LLM output
 *         - Access error.prompt for the original prompt
 *         - Access error.raw_output for the LLM's raw response
 *         - Access error.message for parsing error details
 *      2. BamlClientFinishReasonError: Thrown when LLM terminates with disallowed finish reason
 *         - Access error.prompt for the original prompt
 *         - Access error.raw_output for the LLM's raw response
 *         - Access error.message for error details
 *         - Access error.finish_reason for the specific termination reason
 *      3. Error: Standard JavaScript errors for other cases
 *    - Example error handling:
 *      ```typescript
 *      try {
 *        await mutate(params);
 *      } catch (e) {
 *        if (e instanceof BamlValidationError) {
 *          console.error('Failed to parse LLM output:', e.raw_output);
 *          console.error('Original prompt:', e.prompt);
 *        } else if (e instanceof BamlClientFinishReasonError) {
 *          console.error('LLM terminated early:', e.finish_reason);
 *          console.error('Partial output:', e.raw_output);
 *        } else {
 *          console.error('Other error:', e);
 *        }
 *      }
 *      ```
 *    - Network error detection
 *    - Stream interruption handling
 *
 * 4. Type Safety
 *    - Full TypeScript support
 *    - Inferred types from server actions
 *    - Proper typing for streaming/non-streaming modes
 *
 * @template Action The type of the server action
 * @param serverAction The server action function to execute
 * @param props Configuration props for the hook
 * @returns An object containing the current state and controls
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useBamlAction(myServerAction);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,         // Final result (Action's return type | null)
 *   partialData,  // Latest partial update (RecursivePartialNull<ReturnType> | null)
 *   isPending,    // Whether a request is in progress
 *   isSuccess,    // Whether the last request succeeded
 *   isError,      // Whether the last request failed
 *   error,        // Error object if failed
 *   status,       // 'idle' | 'pending' | 'success' | 'error'
 *   mutate        // Function to trigger the action
 * } = useBamlAction(myServerAction, {
 *   stream: true,
 *
 *   // Called on each partial update
 *   onPartial: (partial) => {
 *     console.log('Partial update:', partial);
 *   },
 *
 *   // Called when streaming completes successfully
 *   onFinal: (final) => {
 *     console.log('Final result:', final);
 *   },
 *
 *   // Called on any error
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making Requests
 * const handleClick = async () => {
 *   try {
 *     const result = await mutate({
 *       // your action's parameters
 *       param1: 'value1',
 *       param2: 'value2'
 *     });
 *
 *     if (result) {
 *       // Handle success
 *     }
 *   } catch (e) {
 *     // Handle errors
 *   }
 * };
 *
 * // 4. Using with React Effects
 * useEffect(() => {
 *   if (data) {
 *     // Handle final data
 *   }
 * }, [data]);
 *
 * useEffect(() => {
 *   if (partialData) {
 *     // Handle streaming updates
 *   }
 * }, [partialData]);
 *
 * // 5. Error Handling
 * useEffect(() => {
 *   if (error) {
 *     console.error('Request failed:', error);
 *   }
 * }, [error]);
 *
 * // 6. Conditional Rendering
 * if (isPending) return <LoadingSpinner />;
 * if (error) return <ErrorMessage error={error} />;
 * if (data) return <SuccessView data={data} />;
 *
 * // 7. Streaming Progress
 * return (
 *   <div>
 *     {isPending && <ProgressIndicator />}
 *     {partialData && <PartialResult data={partialData} />}
 *     {data && <FinalResult data={data} />}
 *
 *     <button
 *       onClick={handleClick}
 *       disabled={isPending}
 *     >
 *       {isPending ? 'Processing...' : 'Start'}
 *     </button>
 *   </div>
 * );
 * ```
 */
export function useBamlAction<Action>(
  action: Action,
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useBamlAction<Action>(
  action: Action,
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useBamlAction<Action>(
  serverAction: Action,
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  const { onFinal, onError, onPartial } = props;
  const isStreaming = isStreamingProps(props);
  const [isPending, startTransition] = useTransition();

  const [state, dispatch] = useReducer(hookReducer<RecursivePartialNull<Awaited<ReturnType<Action>>>, Awaited<ReturnType<Action>>>, {
    isSuccess: false,
    error: null,
    data: null,
    partialData: null,
  });

  const mutate = useCallback(
    async (...input: Parameters<typeof serverAction>) => {
      dispatch({ type: 'START_REQUEST' });

      try {
        let response: Awaited<ReturnType<Action>>;
        await startTransition(async () => {
          response = await serverAction(...input);

          if (isStreaming && response instanceof ReadableStream) {
            const reader = response.getReader();
            const decoder = new TextDecoder();

            try {
              while (true) {
                const { value, done } = await reader.read();

                if (done) break;

                if (value) {
                  const chunk = decoder.decode(value, { stream: true }).trim();
                  try {
                    const parsed: BamlStreamResponse<Awaited<ReturnType<NonStreamingAction>>> = JSON.parse(chunk);

                    if (parsed.error) {
                      // Create appropriate error type
                      let error: Error | BamlValidationError | BamlClientFinishReasonError;
                      if (parsed.error.type === "BamlValidationError") {
                        error = new BamlValidationError(
                          parsed.error.prompt,
                          parsed.error.raw_output,
                          parsed.error.message
                        );
                      } else if (parsed.error.type === "BamlClientFinishReasonError") {
                        error = new BamlClientFinishReasonError(
                          parsed.error.prompt,
                          parsed.error.raw_output,
                          parsed.error.message
                        );
                      } else {
                        error = new Error(parsed.error.message);
                      }
                      throw error;
                    }

                    if (parsed.partial !== undefined) {
                      dispatch({ type: 'SET_PARTIAL', payload: parsed.partial });
                      onPartial?.(parsed.partial);
                    }

                    if (parsed.final !== undefined) {
                      dispatch({ type: 'SET_FINAL', payload: parsed.final });
                      onFinal?.(parsed.final);
                      return parsed.final;
                    }
                  } catch (err) {
                    dispatch({ type: "SET_ERROR", payload: err });
                    onError?.(err);
                    break;
                  }
                }
              }
            } finally {
              reader.releaseLock();
            }
            return response;
          }

          // Non-streaming case
          dispatch({ type: 'SET_FINAL', payload: response });
          onFinal?.(response);
        });
        return response;
      } catch (error_) {
        dispatch({ type: 'SET_ERROR', payload: error_ });
        onError?.(error_);
        throw error_;
      }
    },
    [serverAction, isStreaming, onPartial, onFinal, onError],
  );

  const status = useMemo<"idle" | "pending" | "success" | "error">(() => {
    if (isPending) return "pending";
    if (state.error) return "error";
    if (state.isSuccess) return "success";
    return "idle";
  }, [isPending, state.error, state.isSuccess]);

  const result = {
    data: state.data,
    error: state.error,
    isError: state.error !== null,
    isSuccess: state.isSuccess,
    isPending,
    mutate,
    status,
  };

  return {
    ...result,
    partialData: isStreaming ? state.partialData : undefined,
  };
}
/**
 * A specialized hook for the AaaSamOutputFormat BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - recipe: types.string
 *
 *
 * Return Type:
 * - Non-streaming: Recipe
 * - Streaming Partial: RecursivePartialNull<Recipe>
 * - Streaming Final: Recipe
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useAaaSamOutputFormat();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Recipe
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Recipe | null
 *   partialData, // Type: RecursivePartialNull<Recipe> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useAaaSamOutputFormat({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       recipe: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       recipe: firstValue as string,
 *     }),
 *     mutate({
 *       recipe: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useAaaSamOutputFormat<Action extends typeof ServerActions.AaaSamOutputFormatAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useAaaSamOutputFormat<Action extends typeof ServerActions.AaaSamOutputFormatAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useAaaSamOutputFormat<Action extends typeof ServerActions.AaaSamOutputFormatAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.AaaSamOutputFormatStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.AaaSamOutputFormatAction,
    props
  );
}

/**
 * A specialized hook for the AliasThatPointsToRecursiveType BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - list: types.LinkedListAliasNode
 *
 *
 * Return Type:
 * - Non-streaming: LinkedListAliasNode
 * - Streaming Partial: RecursivePartialNull<LinkedListAliasNode>
 * - Streaming Final: LinkedListAliasNode
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useAliasThatPointsToRecursiveType();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to LinkedListAliasNode
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: LinkedListAliasNode | null
 *   partialData, // Type: RecursivePartialNull<LinkedListAliasNode> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useAliasThatPointsToRecursiveType({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       list: someValue as LinkedListAliasNode,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       list: firstValue as LinkedListAliasNode,
 *     }),
 *     mutate({
 *       list: secondValue as LinkedListAliasNode,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useAliasThatPointsToRecursiveType<Action extends typeof ServerActions.AliasThatPointsToRecursiveTypeAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useAliasThatPointsToRecursiveType<Action extends typeof ServerActions.AliasThatPointsToRecursiveTypeAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useAliasThatPointsToRecursiveType<Action extends typeof ServerActions.AliasThatPointsToRecursiveTypeAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.AliasThatPointsToRecursiveTypeStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.AliasThatPointsToRecursiveTypeAction,
    props
  );
}

/**
 * A specialized hook for the AliasWithMultipleAttrs BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - money: types.Checked<number,"gt_ten">
 *
 *
 * Return Type:
 * - Non-streaming: Checked<number,"gt_ten">
 * - Streaming Partial: RecursivePartialNull<Checked<number,"gt_ten">>
 * - Streaming Final: Checked<number,"gt_ten">
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useAliasWithMultipleAttrs();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Checked<number,"gt_ten">
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Checked<number,"gt_ten"> | null
 *   partialData, // Type: RecursivePartialNull<Checked<number,"gt_ten">> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useAliasWithMultipleAttrs({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       money: someValue as Checked<number,"gt_ten">,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       money: firstValue as Checked<number,"gt_ten">,
 *     }),
 *     mutate({
 *       money: secondValue as Checked<number,"gt_ten">,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useAliasWithMultipleAttrs<Action extends typeof ServerActions.AliasWithMultipleAttrsAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useAliasWithMultipleAttrs<Action extends typeof ServerActions.AliasWithMultipleAttrsAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useAliasWithMultipleAttrs<Action extends typeof ServerActions.AliasWithMultipleAttrsAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.AliasWithMultipleAttrsStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.AliasWithMultipleAttrsAction,
    props
  );
}

/**
 * A specialized hook for the AliasedInputClass BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.InputClass
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useAliasedInputClass();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useAliasedInputClass({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as InputClass,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as InputClass,
 *     }),
 *     mutate({
 *       input: secondValue as InputClass,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useAliasedInputClass<Action extends typeof ServerActions.AliasedInputClassAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useAliasedInputClass<Action extends typeof ServerActions.AliasedInputClassAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useAliasedInputClass<Action extends typeof ServerActions.AliasedInputClassAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.AliasedInputClassStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.AliasedInputClassAction,
    props
  );
}

/**
 * A specialized hook for the AliasedInputClass2 BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.InputClass
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useAliasedInputClass2();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useAliasedInputClass2({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as InputClass,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as InputClass,
 *     }),
 *     mutate({
 *       input: secondValue as InputClass,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useAliasedInputClass2<Action extends typeof ServerActions.AliasedInputClass2Action>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useAliasedInputClass2<Action extends typeof ServerActions.AliasedInputClass2Action>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useAliasedInputClass2<Action extends typeof ServerActions.AliasedInputClass2Action>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.AliasedInputClass2StreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.AliasedInputClass2Action,
    props
  );
}

/**
 * A specialized hook for the AliasedInputClassNested BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.InputClassNested
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useAliasedInputClassNested();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useAliasedInputClassNested({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as InputClassNested,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as InputClassNested,
 *     }),
 *     mutate({
 *       input: secondValue as InputClassNested,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useAliasedInputClassNested<Action extends typeof ServerActions.AliasedInputClassNestedAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useAliasedInputClassNested<Action extends typeof ServerActions.AliasedInputClassNestedAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useAliasedInputClassNested<Action extends typeof ServerActions.AliasedInputClassNestedAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.AliasedInputClassNestedStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.AliasedInputClassNestedAction,
    props
  );
}

/**
 * A specialized hook for the AliasedInputEnum BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.AliasedEnum
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useAliasedInputEnum();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useAliasedInputEnum({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as AliasedEnum,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as AliasedEnum,
 *     }),
 *     mutate({
 *       input: secondValue as AliasedEnum,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useAliasedInputEnum<Action extends typeof ServerActions.AliasedInputEnumAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useAliasedInputEnum<Action extends typeof ServerActions.AliasedInputEnumAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useAliasedInputEnum<Action extends typeof ServerActions.AliasedInputEnumAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.AliasedInputEnumStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.AliasedInputEnumAction,
    props
  );
}

/**
 * A specialized hook for the AliasedInputList BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.AliasedEnum[]
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useAliasedInputList();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useAliasedInputList({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as AliasedEnum[],  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as AliasedEnum[],
 *     }),
 *     mutate({
 *       input: secondValue as AliasedEnum[],
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useAliasedInputList<Action extends typeof ServerActions.AliasedInputListAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useAliasedInputList<Action extends typeof ServerActions.AliasedInputListAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useAliasedInputList<Action extends typeof ServerActions.AliasedInputListAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.AliasedInputListStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.AliasedInputListAction,
    props
  );
}

/**
 * A specialized hook for the AllowedOptionals BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - optionals: types.OptionalListAndMap
 *
 *
 * Return Type:
 * - Non-streaming: OptionalListAndMap
 * - Streaming Partial: RecursivePartialNull<OptionalListAndMap>
 * - Streaming Final: OptionalListAndMap
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useAllowedOptionals();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to OptionalListAndMap
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: OptionalListAndMap | null
 *   partialData, // Type: RecursivePartialNull<OptionalListAndMap> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useAllowedOptionals({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       optionals: someValue as OptionalListAndMap,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       optionals: firstValue as OptionalListAndMap,
 *     }),
 *     mutate({
 *       optionals: secondValue as OptionalListAndMap,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useAllowedOptionals<Action extends typeof ServerActions.AllowedOptionalsAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useAllowedOptionals<Action extends typeof ServerActions.AllowedOptionalsAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useAllowedOptionals<Action extends typeof ServerActions.AllowedOptionalsAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.AllowedOptionalsStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.AllowedOptionalsAction,
    props
  );
}

/**
 * A specialized hook for the AssertFn BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - a: types.number
 *
 *
 * Return Type:
 * - Non-streaming: number
 * - Streaming Partial: RecursivePartialNull<number>
 * - Streaming Final: number
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useAssertFn();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to number
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: number | null
 *   partialData, // Type: RecursivePartialNull<number> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useAssertFn({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       a: someValue as number,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       a: firstValue as number,
 *     }),
 *     mutate({
 *       a: secondValue as number,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useAssertFn<Action extends typeof ServerActions.AssertFnAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useAssertFn<Action extends typeof ServerActions.AssertFnAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useAssertFn<Action extends typeof ServerActions.AssertFnAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.AssertFnStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.AssertFnAction,
    props
  );
}

/**
 * A specialized hook for the AudioInput BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - aud: types.Audio
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useAudioInput();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useAudioInput({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       aud: someValue as Audio,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       aud: firstValue as Audio,
 *     }),
 *     mutate({
 *       aud: secondValue as Audio,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useAudioInput<Action extends typeof ServerActions.AudioInputAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useAudioInput<Action extends typeof ServerActions.AudioInputAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useAudioInput<Action extends typeof ServerActions.AudioInputAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.AudioInputStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.AudioInputAction,
    props
  );
}

/**
 * A specialized hook for the BuildLinkedList BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.number[]
 *
 *
 * Return Type:
 * - Non-streaming: LinkedList
 * - Streaming Partial: RecursivePartialNull<LinkedList>
 * - Streaming Final: LinkedList
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useBuildLinkedList();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to LinkedList
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: LinkedList | null
 *   partialData, // Type: RecursivePartialNull<LinkedList> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useBuildLinkedList({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as number[],  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as number[],
 *     }),
 *     mutate({
 *       input: secondValue as number[],
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useBuildLinkedList<Action extends typeof ServerActions.BuildLinkedListAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useBuildLinkedList<Action extends typeof ServerActions.BuildLinkedListAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useBuildLinkedList<Action extends typeof ServerActions.BuildLinkedListAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.BuildLinkedListStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.BuildLinkedListAction,
    props
  );
}

/**
 * A specialized hook for the BuildTree BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.BinaryNode
 *
 *
 * Return Type:
 * - Non-streaming: Tree
 * - Streaming Partial: RecursivePartialNull<Tree>
 * - Streaming Final: Tree
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useBuildTree();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Tree
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Tree | null
 *   partialData, // Type: RecursivePartialNull<Tree> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useBuildTree({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as BinaryNode,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as BinaryNode,
 *     }),
 *     mutate({
 *       input: secondValue as BinaryNode,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useBuildTree<Action extends typeof ServerActions.BuildTreeAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useBuildTree<Action extends typeof ServerActions.BuildTreeAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useBuildTree<Action extends typeof ServerActions.BuildTreeAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.BuildTreeStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.BuildTreeAction,
    props
  );
}

/**
 * A specialized hook for the ClassThatPointsToRecursiveClassThroughAlias BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - cls: types.ClassToRecAlias
 *
 *
 * Return Type:
 * - Non-streaming: ClassToRecAlias
 * - Streaming Partial: RecursivePartialNull<ClassToRecAlias>
 * - Streaming Final: ClassToRecAlias
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useClassThatPointsToRecursiveClassThroughAlias();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to ClassToRecAlias
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: ClassToRecAlias | null
 *   partialData, // Type: RecursivePartialNull<ClassToRecAlias> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useClassThatPointsToRecursiveClassThroughAlias({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       cls: someValue as ClassToRecAlias,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       cls: firstValue as ClassToRecAlias,
 *     }),
 *     mutate({
 *       cls: secondValue as ClassToRecAlias,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useClassThatPointsToRecursiveClassThroughAlias<Action extends typeof ServerActions.ClassThatPointsToRecursiveClassThroughAliasAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useClassThatPointsToRecursiveClassThroughAlias<Action extends typeof ServerActions.ClassThatPointsToRecursiveClassThroughAliasAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useClassThatPointsToRecursiveClassThroughAlias<Action extends typeof ServerActions.ClassThatPointsToRecursiveClassThroughAliasAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ClassThatPointsToRecursiveClassThroughAliasStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ClassThatPointsToRecursiveClassThroughAliasAction,
    props
  );
}

/**
 * A specialized hook for the ClassifyDynEnumTwo BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: (string | DynEnumTwo)
 * - Streaming Partial: RecursivePartialNull<(string | DynEnumTwo)>
 * - Streaming Final: (string | DynEnumTwo)
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useClassifyDynEnumTwo();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to (string | DynEnumTwo)
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: (string | DynEnumTwo) | null
 *   partialData, // Type: RecursivePartialNull<(string | DynEnumTwo)> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useClassifyDynEnumTwo({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useClassifyDynEnumTwo<Action extends typeof ServerActions.ClassifyDynEnumTwoAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useClassifyDynEnumTwo<Action extends typeof ServerActions.ClassifyDynEnumTwoAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useClassifyDynEnumTwo<Action extends typeof ServerActions.ClassifyDynEnumTwoAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ClassifyDynEnumTwoStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ClassifyDynEnumTwoAction,
    props
  );
}

/**
 * A specialized hook for the ClassifyMessage BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: Category
 * - Streaming Partial: RecursivePartialNull<Category>
 * - Streaming Final: Category
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useClassifyMessage();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Category
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Category | null
 *   partialData, // Type: RecursivePartialNull<Category> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useClassifyMessage({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useClassifyMessage<Action extends typeof ServerActions.ClassifyMessageAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useClassifyMessage<Action extends typeof ServerActions.ClassifyMessageAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useClassifyMessage<Action extends typeof ServerActions.ClassifyMessageAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ClassifyMessageStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ClassifyMessageAction,
    props
  );
}

/**
 * A specialized hook for the ClassifyMessage2 BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: Category
 * - Streaming Partial: RecursivePartialNull<Category>
 * - Streaming Final: Category
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useClassifyMessage2();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Category
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Category | null
 *   partialData, // Type: RecursivePartialNull<Category> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useClassifyMessage2({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useClassifyMessage2<Action extends typeof ServerActions.ClassifyMessage2Action>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useClassifyMessage2<Action extends typeof ServerActions.ClassifyMessage2Action>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useClassifyMessage2<Action extends typeof ServerActions.ClassifyMessage2Action>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ClassifyMessage2StreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ClassifyMessage2Action,
    props
  );
}

/**
 * A specialized hook for the ClassifyMessage3 BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: Category
 * - Streaming Partial: RecursivePartialNull<Category>
 * - Streaming Final: Category
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useClassifyMessage3();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Category
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Category | null
 *   partialData, // Type: RecursivePartialNull<Category> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useClassifyMessage3({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useClassifyMessage3<Action extends typeof ServerActions.ClassifyMessage3Action>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useClassifyMessage3<Action extends typeof ServerActions.ClassifyMessage3Action>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useClassifyMessage3<Action extends typeof ServerActions.ClassifyMessage3Action>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ClassifyMessage3StreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ClassifyMessage3Action,
    props
  );
}

/**
 * A specialized hook for the Completion BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - prefix: types.string
 *
 * - suffix: types.string
 *
 * - language: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useCompletion();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useCompletion({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       prefix: someValue as string,  // Replace someValue with your data
 *       suffix: someValue as string,  // Replace someValue with your data
 *       language: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       prefix: firstValue as string,
 *       suffix: firstValue as string,
 *       language: firstValue as string,
 *     }),
 *     mutate({
 *       prefix: secondValue as string,
 *       suffix: secondValue as string,
 *       language: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useCompletion<Action extends typeof ServerActions.CompletionAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useCompletion<Action extends typeof ServerActions.CompletionAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useCompletion<Action extends typeof ServerActions.CompletionAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.CompletionStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.CompletionAction,
    props
  );
}

/**
 * A specialized hook for the CustomTask BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: BookOrder | FlightConfirmation | GroceryReceipt
 * - Streaming Partial: RecursivePartialNull<BookOrder | FlightConfirmation | GroceryReceipt>
 * - Streaming Final: BookOrder | FlightConfirmation | GroceryReceipt
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useCustomTask();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to BookOrder | FlightConfirmation | GroceryReceipt
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: BookOrder | FlightConfirmation | GroceryReceipt | null
 *   partialData, // Type: RecursivePartialNull<BookOrder | FlightConfirmation | GroceryReceipt> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useCustomTask({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useCustomTask<Action extends typeof ServerActions.CustomTaskAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useCustomTask<Action extends typeof ServerActions.CustomTaskAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useCustomTask<Action extends typeof ServerActions.CustomTaskAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.CustomTaskStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.CustomTaskAction,
    props
  );
}

/**
 * A specialized hook for the DescribeImage BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - img: types.Image
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useDescribeImage();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useDescribeImage({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       img: someValue as Image,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       img: firstValue as Image,
 *     }),
 *     mutate({
 *       img: secondValue as Image,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useDescribeImage<Action extends typeof ServerActions.DescribeImageAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useDescribeImage<Action extends typeof ServerActions.DescribeImageAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useDescribeImage<Action extends typeof ServerActions.DescribeImageAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.DescribeImageStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.DescribeImageAction,
    props
  );
}

/**
 * A specialized hook for the DescribeImage2 BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - classWithImage: types.ClassWithImage
 *
 * - img2: types.Image
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useDescribeImage2();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useDescribeImage2({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       classWithImage: someValue as ClassWithImage,  // Replace someValue with your data
 *       img2: someValue as Image,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       classWithImage: firstValue as ClassWithImage,
 *       img2: firstValue as Image,
 *     }),
 *     mutate({
 *       classWithImage: secondValue as ClassWithImage,
 *       img2: secondValue as Image,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useDescribeImage2<Action extends typeof ServerActions.DescribeImage2Action>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useDescribeImage2<Action extends typeof ServerActions.DescribeImage2Action>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useDescribeImage2<Action extends typeof ServerActions.DescribeImage2Action>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.DescribeImage2StreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.DescribeImage2Action,
    props
  );
}

/**
 * A specialized hook for the DescribeImage3 BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - classWithImage: types.ClassWithImage
 *
 * - img2: types.Image
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useDescribeImage3();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useDescribeImage3({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       classWithImage: someValue as ClassWithImage,  // Replace someValue with your data
 *       img2: someValue as Image,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       classWithImage: firstValue as ClassWithImage,
 *       img2: firstValue as Image,
 *     }),
 *     mutate({
 *       classWithImage: secondValue as ClassWithImage,
 *       img2: secondValue as Image,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useDescribeImage3<Action extends typeof ServerActions.DescribeImage3Action>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useDescribeImage3<Action extends typeof ServerActions.DescribeImage3Action>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useDescribeImage3<Action extends typeof ServerActions.DescribeImage3Action>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.DescribeImage3StreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.DescribeImage3Action,
    props
  );
}

/**
 * A specialized hook for the DescribeImage4 BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - classWithImage: types.ClassWithImage
 *
 * - img2: types.Image
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useDescribeImage4();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useDescribeImage4({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       classWithImage: someValue as ClassWithImage,  // Replace someValue with your data
 *       img2: someValue as Image,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       classWithImage: firstValue as ClassWithImage,
 *       img2: firstValue as Image,
 *     }),
 *     mutate({
 *       classWithImage: secondValue as ClassWithImage,
 *       img2: secondValue as Image,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useDescribeImage4<Action extends typeof ServerActions.DescribeImage4Action>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useDescribeImage4<Action extends typeof ServerActions.DescribeImage4Action>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useDescribeImage4<Action extends typeof ServerActions.DescribeImage4Action>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.DescribeImage4StreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.DescribeImage4Action,
    props
  );
}

/**
 * A specialized hook for the DifferentiateUnions BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 *
 * Return Type:
 * - Non-streaming: OriginalA | OriginalB
 * - Streaming Partial: RecursivePartialNull<OriginalA | OriginalB>
 * - Streaming Final: OriginalA | OriginalB
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useDifferentiateUnions();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to OriginalA | OriginalB
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: OriginalA | OriginalB | null
 *   partialData, // Type: RecursivePartialNull<OriginalA | OriginalB> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useDifferentiateUnions({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *     }),
 *     mutate({
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useDifferentiateUnions<Action extends typeof ServerActions.DifferentiateUnionsAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useDifferentiateUnions<Action extends typeof ServerActions.DifferentiateUnionsAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useDifferentiateUnions<Action extends typeof ServerActions.DifferentiateUnionsAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.DifferentiateUnionsStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.DifferentiateUnionsAction,
    props
  );
}

/**
 * A specialized hook for the DummyOutputFunction BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: DummyOutput
 * - Streaming Partial: RecursivePartialNull<DummyOutput>
 * - Streaming Final: DummyOutput
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useDummyOutputFunction();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to DummyOutput
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: DummyOutput | null
 *   partialData, // Type: RecursivePartialNull<DummyOutput> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useDummyOutputFunction({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useDummyOutputFunction<Action extends typeof ServerActions.DummyOutputFunctionAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useDummyOutputFunction<Action extends typeof ServerActions.DummyOutputFunctionAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useDummyOutputFunction<Action extends typeof ServerActions.DummyOutputFunctionAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.DummyOutputFunctionStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.DummyOutputFunctionAction,
    props
  );
}

/**
 * A specialized hook for the DynamicFunc BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.DynamicClassOne
 *
 *
 * Return Type:
 * - Non-streaming: DynamicClassTwo
 * - Streaming Partial: RecursivePartialNull<DynamicClassTwo>
 * - Streaming Final: DynamicClassTwo
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useDynamicFunc();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to DynamicClassTwo
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: DynamicClassTwo | null
 *   partialData, // Type: RecursivePartialNull<DynamicClassTwo> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useDynamicFunc({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as DynamicClassOne,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as DynamicClassOne,
 *     }),
 *     mutate({
 *       input: secondValue as DynamicClassOne,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useDynamicFunc<Action extends typeof ServerActions.DynamicFuncAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useDynamicFunc<Action extends typeof ServerActions.DynamicFuncAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useDynamicFunc<Action extends typeof ServerActions.DynamicFuncAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.DynamicFuncStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.DynamicFuncAction,
    props
  );
}

/**
 * A specialized hook for the DynamicInputOutput BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.DynInputOutput
 *
 *
 * Return Type:
 * - Non-streaming: DynInputOutput
 * - Streaming Partial: RecursivePartialNull<DynInputOutput>
 * - Streaming Final: DynInputOutput
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useDynamicInputOutput();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to DynInputOutput
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: DynInputOutput | null
 *   partialData, // Type: RecursivePartialNull<DynInputOutput> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useDynamicInputOutput({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as DynInputOutput,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as DynInputOutput,
 *     }),
 *     mutate({
 *       input: secondValue as DynInputOutput,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useDynamicInputOutput<Action extends typeof ServerActions.DynamicInputOutputAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useDynamicInputOutput<Action extends typeof ServerActions.DynamicInputOutputAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useDynamicInputOutput<Action extends typeof ServerActions.DynamicInputOutputAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.DynamicInputOutputStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.DynamicInputOutputAction,
    props
  );
}

/**
 * A specialized hook for the DynamicListInputOutput BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.DynInputOutput[]
 *
 *
 * Return Type:
 * - Non-streaming: DynInputOutput[]
 * - Streaming Partial: RecursivePartialNull<DynInputOutput[]>
 * - Streaming Final: DynInputOutput[]
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useDynamicListInputOutput();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to DynInputOutput[]
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: DynInputOutput[] | null
 *   partialData, // Type: RecursivePartialNull<DynInputOutput[]> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useDynamicListInputOutput({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as DynInputOutput[],  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as DynInputOutput[],
 *     }),
 *     mutate({
 *       input: secondValue as DynInputOutput[],
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useDynamicListInputOutput<Action extends typeof ServerActions.DynamicListInputOutputAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useDynamicListInputOutput<Action extends typeof ServerActions.DynamicListInputOutputAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useDynamicListInputOutput<Action extends typeof ServerActions.DynamicListInputOutputAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.DynamicListInputOutputStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.DynamicListInputOutputAction,
    props
  );
}

/**
 * A specialized hook for the ExpectFailure BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useExpectFailure();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useExpectFailure({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *     }),
 *     mutate({
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useExpectFailure<Action extends typeof ServerActions.ExpectFailureAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useExpectFailure<Action extends typeof ServerActions.ExpectFailureAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useExpectFailure<Action extends typeof ServerActions.ExpectFailureAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ExpectFailureStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ExpectFailureAction,
    props
  );
}

/**
 * A specialized hook for the ExtractContactInfo BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - document: types.string
 *
 *
 * Return Type:
 * - Non-streaming: ContactInfo
 * - Streaming Partial: RecursivePartialNull<ContactInfo>
 * - Streaming Final: ContactInfo
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useExtractContactInfo();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to ContactInfo
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: ContactInfo | null
 *   partialData, // Type: RecursivePartialNull<ContactInfo> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useExtractContactInfo({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       document: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       document: firstValue as string,
 *     }),
 *     mutate({
 *       document: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useExtractContactInfo<Action extends typeof ServerActions.ExtractContactInfoAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useExtractContactInfo<Action extends typeof ServerActions.ExtractContactInfoAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useExtractContactInfo<Action extends typeof ServerActions.ExtractContactInfoAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ExtractContactInfoStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ExtractContactInfoAction,
    props
  );
}

/**
 * A specialized hook for the ExtractHobby BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - text: types.string
 *
 *
 * Return Type:
 * - Non-streaming: (string | Hobby)[]
 * - Streaming Partial: RecursivePartialNull<(string | Hobby)[]>
 * - Streaming Final: (string | Hobby)[]
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useExtractHobby();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to (string | Hobby)[]
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: (string | Hobby)[] | null
 *   partialData, // Type: RecursivePartialNull<(string | Hobby)[]> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useExtractHobby({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       text: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       text: firstValue as string,
 *     }),
 *     mutate({
 *       text: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useExtractHobby<Action extends typeof ServerActions.ExtractHobbyAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useExtractHobby<Action extends typeof ServerActions.ExtractHobbyAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useExtractHobby<Action extends typeof ServerActions.ExtractHobbyAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ExtractHobbyStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ExtractHobbyAction,
    props
  );
}

/**
 * A specialized hook for the ExtractNames BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string[]
 * - Streaming Partial: RecursivePartialNull<string[]>
 * - Streaming Final: string[]
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useExtractNames();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string[]
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string[] | null
 *   partialData, // Type: RecursivePartialNull<string[]> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useExtractNames({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useExtractNames<Action extends typeof ServerActions.ExtractNamesAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useExtractNames<Action extends typeof ServerActions.ExtractNamesAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useExtractNames<Action extends typeof ServerActions.ExtractNamesAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ExtractNamesStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ExtractNamesAction,
    props
  );
}

/**
 * A specialized hook for the ExtractPeople BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - text: types.string
 *
 *
 * Return Type:
 * - Non-streaming: Person[]
 * - Streaming Partial: RecursivePartialNull<Person[]>
 * - Streaming Final: Person[]
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useExtractPeople();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Person[]
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Person[] | null
 *   partialData, // Type: RecursivePartialNull<Person[]> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useExtractPeople({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       text: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       text: firstValue as string,
 *     }),
 *     mutate({
 *       text: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useExtractPeople<Action extends typeof ServerActions.ExtractPeopleAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useExtractPeople<Action extends typeof ServerActions.ExtractPeopleAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useExtractPeople<Action extends typeof ServerActions.ExtractPeopleAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ExtractPeopleStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ExtractPeopleAction,
    props
  );
}

/**
 * A specialized hook for the ExtractReceiptInfo BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - email: types.string
 *
 * - reason: types."curiosity" | "personal_finance"
 *
 *
 * Return Type:
 * - Non-streaming: ReceiptInfo
 * - Streaming Partial: RecursivePartialNull<ReceiptInfo>
 * - Streaming Final: ReceiptInfo
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useExtractReceiptInfo();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to ReceiptInfo
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: ReceiptInfo | null
 *   partialData, // Type: RecursivePartialNull<ReceiptInfo> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useExtractReceiptInfo({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       email: someValue as string,  // Replace someValue with your data
 *       reason: someValue as "curiosity" | "personal_finance",  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       email: firstValue as string,
 *       reason: firstValue as "curiosity" | "personal_finance",
 *     }),
 *     mutate({
 *       email: secondValue as string,
 *       reason: secondValue as "curiosity" | "personal_finance",
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useExtractReceiptInfo<Action extends typeof ServerActions.ExtractReceiptInfoAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useExtractReceiptInfo<Action extends typeof ServerActions.ExtractReceiptInfoAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useExtractReceiptInfo<Action extends typeof ServerActions.ExtractReceiptInfoAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ExtractReceiptInfoStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ExtractReceiptInfoAction,
    props
  );
}

/**
 * A specialized hook for the ExtractResume BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - resume: types.string
 *
 * - img (optional): types.Image | null
 *
 *
 * Return Type:
 * - Non-streaming: Resume
 * - Streaming Partial: RecursivePartialNull<Resume>
 * - Streaming Final: Resume
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useExtractResume();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Resume
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Resume | null
 *   partialData, // Type: RecursivePartialNull<Resume> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useExtractResume({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       resume: someValue as string,  // Replace someValue with your data
 *       img: someValue as Image | null,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       resume: firstValue as string,
 *       img: firstValue as Image | null,
 *     }),
 *     mutate({
 *       resume: secondValue as string,
 *       img: secondValue as Image | null,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useExtractResume<Action extends typeof ServerActions.ExtractResumeAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useExtractResume<Action extends typeof ServerActions.ExtractResumeAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useExtractResume<Action extends typeof ServerActions.ExtractResumeAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ExtractResumeStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ExtractResumeAction,
    props
  );
}

/**
 * A specialized hook for the ExtractResume2 BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - resume: types.string
 *
 *
 * Return Type:
 * - Non-streaming: Resume
 * - Streaming Partial: RecursivePartialNull<Resume>
 * - Streaming Final: Resume
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useExtractResume2();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Resume
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Resume | null
 *   partialData, // Type: RecursivePartialNull<Resume> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useExtractResume2({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       resume: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       resume: firstValue as string,
 *     }),
 *     mutate({
 *       resume: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useExtractResume2<Action extends typeof ServerActions.ExtractResume2Action>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useExtractResume2<Action extends typeof ServerActions.ExtractResume2Action>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useExtractResume2<Action extends typeof ServerActions.ExtractResume2Action>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ExtractResume2StreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ExtractResume2Action,
    props
  );
}

/**
 * A specialized hook for the FnClassOptionalOutput BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: ClassOptionalOutput | null
 * - Streaming Partial: RecursivePartialNull<ClassOptionalOutput | null>
 * - Streaming Final: ClassOptionalOutput | null
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnClassOptionalOutput();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to ClassOptionalOutput | null
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: ClassOptionalOutput | null | null
 *   partialData, // Type: RecursivePartialNull<ClassOptionalOutput | null> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnClassOptionalOutput({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnClassOptionalOutput<Action extends typeof ServerActions.FnClassOptionalOutputAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnClassOptionalOutput<Action extends typeof ServerActions.FnClassOptionalOutputAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnClassOptionalOutput<Action extends typeof ServerActions.FnClassOptionalOutputAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnClassOptionalOutputStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnClassOptionalOutputAction,
    props
  );
}

/**
 * A specialized hook for the FnClassOptionalOutput2 BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: ClassOptionalOutput2 | null
 * - Streaming Partial: RecursivePartialNull<ClassOptionalOutput2 | null>
 * - Streaming Final: ClassOptionalOutput2 | null
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnClassOptionalOutput2();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to ClassOptionalOutput2 | null
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: ClassOptionalOutput2 | null | null
 *   partialData, // Type: RecursivePartialNull<ClassOptionalOutput2 | null> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnClassOptionalOutput2({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnClassOptionalOutput2<Action extends typeof ServerActions.FnClassOptionalOutput2Action>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnClassOptionalOutput2<Action extends typeof ServerActions.FnClassOptionalOutput2Action>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnClassOptionalOutput2<Action extends typeof ServerActions.FnClassOptionalOutput2Action>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnClassOptionalOutput2StreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnClassOptionalOutput2Action,
    props
  );
}

/**
 * A specialized hook for the FnEnumListOutput BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: EnumOutput[]
 * - Streaming Partial: RecursivePartialNull<EnumOutput[]>
 * - Streaming Final: EnumOutput[]
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnEnumListOutput();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to EnumOutput[]
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: EnumOutput[] | null
 *   partialData, // Type: RecursivePartialNull<EnumOutput[]> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnEnumListOutput({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnEnumListOutput<Action extends typeof ServerActions.FnEnumListOutputAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnEnumListOutput<Action extends typeof ServerActions.FnEnumListOutputAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnEnumListOutput<Action extends typeof ServerActions.FnEnumListOutputAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnEnumListOutputStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnEnumListOutputAction,
    props
  );
}

/**
 * A specialized hook for the FnEnumOutput BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: EnumOutput
 * - Streaming Partial: RecursivePartialNull<EnumOutput>
 * - Streaming Final: EnumOutput
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnEnumOutput();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to EnumOutput
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: EnumOutput | null
 *   partialData, // Type: RecursivePartialNull<EnumOutput> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnEnumOutput({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnEnumOutput<Action extends typeof ServerActions.FnEnumOutputAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnEnumOutput<Action extends typeof ServerActions.FnEnumOutputAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnEnumOutput<Action extends typeof ServerActions.FnEnumOutputAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnEnumOutputStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnEnumOutputAction,
    props
  );
}

/**
 * A specialized hook for the FnLiteralClassInputOutput BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.LiteralClassHello
 *
 *
 * Return Type:
 * - Non-streaming: LiteralClassHello
 * - Streaming Partial: RecursivePartialNull<LiteralClassHello>
 * - Streaming Final: LiteralClassHello
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnLiteralClassInputOutput();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to LiteralClassHello
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: LiteralClassHello | null
 *   partialData, // Type: RecursivePartialNull<LiteralClassHello> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnLiteralClassInputOutput({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as LiteralClassHello,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as LiteralClassHello,
 *     }),
 *     mutate({
 *       input: secondValue as LiteralClassHello,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnLiteralClassInputOutput<Action extends typeof ServerActions.FnLiteralClassInputOutputAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnLiteralClassInputOutput<Action extends typeof ServerActions.FnLiteralClassInputOutputAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnLiteralClassInputOutput<Action extends typeof ServerActions.FnLiteralClassInputOutputAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnLiteralClassInputOutputStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnLiteralClassInputOutputAction,
    props
  );
}

/**
 * A specialized hook for the FnLiteralUnionClassInputOutput BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.LiteralClassOne | LiteralClassTwo
 *
 *
 * Return Type:
 * - Non-streaming: LiteralClassOne | LiteralClassTwo
 * - Streaming Partial: RecursivePartialNull<LiteralClassOne | LiteralClassTwo>
 * - Streaming Final: LiteralClassOne | LiteralClassTwo
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnLiteralUnionClassInputOutput();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to LiteralClassOne | LiteralClassTwo
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: LiteralClassOne | LiteralClassTwo | null
 *   partialData, // Type: RecursivePartialNull<LiteralClassOne | LiteralClassTwo> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnLiteralUnionClassInputOutput({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as LiteralClassOne | LiteralClassTwo,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as LiteralClassOne | LiteralClassTwo,
 *     }),
 *     mutate({
 *       input: secondValue as LiteralClassOne | LiteralClassTwo,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnLiteralUnionClassInputOutput<Action extends typeof ServerActions.FnLiteralUnionClassInputOutputAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnLiteralUnionClassInputOutput<Action extends typeof ServerActions.FnLiteralUnionClassInputOutputAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnLiteralUnionClassInputOutput<Action extends typeof ServerActions.FnLiteralUnionClassInputOutputAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnLiteralUnionClassInputOutputStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnLiteralUnionClassInputOutputAction,
    props
  );
}

/**
 * A specialized hook for the FnNamedArgsSingleStringOptional BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myString (optional): types.string | null
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnNamedArgsSingleStringOptional();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnNamedArgsSingleStringOptional({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myString: someValue as string | null,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myString: firstValue as string | null,
 *     }),
 *     mutate({
 *       myString: secondValue as string | null,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnNamedArgsSingleStringOptional<Action extends typeof ServerActions.FnNamedArgsSingleStringOptionalAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnNamedArgsSingleStringOptional<Action extends typeof ServerActions.FnNamedArgsSingleStringOptionalAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnNamedArgsSingleStringOptional<Action extends typeof ServerActions.FnNamedArgsSingleStringOptionalAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnNamedArgsSingleStringOptionalStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnNamedArgsSingleStringOptionalAction,
    props
  );
}

/**
 * A specialized hook for the FnOutputBool BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: boolean
 * - Streaming Partial: RecursivePartialNull<boolean>
 * - Streaming Final: boolean
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnOutputBool();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to boolean
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: boolean | null
 *   partialData, // Type: RecursivePartialNull<boolean> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnOutputBool({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnOutputBool<Action extends typeof ServerActions.FnOutputBoolAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnOutputBool<Action extends typeof ServerActions.FnOutputBoolAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnOutputBool<Action extends typeof ServerActions.FnOutputBoolAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnOutputBoolStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnOutputBoolAction,
    props
  );
}

/**
 * A specialized hook for the FnOutputClass BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: TestOutputClass
 * - Streaming Partial: RecursivePartialNull<TestOutputClass>
 * - Streaming Final: TestOutputClass
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnOutputClass();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to TestOutputClass
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: TestOutputClass | null
 *   partialData, // Type: RecursivePartialNull<TestOutputClass> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnOutputClass({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnOutputClass<Action extends typeof ServerActions.FnOutputClassAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnOutputClass<Action extends typeof ServerActions.FnOutputClassAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnOutputClass<Action extends typeof ServerActions.FnOutputClassAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnOutputClassStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnOutputClassAction,
    props
  );
}

/**
 * A specialized hook for the FnOutputClassList BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: TestOutputClass[]
 * - Streaming Partial: RecursivePartialNull<TestOutputClass[]>
 * - Streaming Final: TestOutputClass[]
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnOutputClassList();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to TestOutputClass[]
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: TestOutputClass[] | null
 *   partialData, // Type: RecursivePartialNull<TestOutputClass[]> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnOutputClassList({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnOutputClassList<Action extends typeof ServerActions.FnOutputClassListAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnOutputClassList<Action extends typeof ServerActions.FnOutputClassListAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnOutputClassList<Action extends typeof ServerActions.FnOutputClassListAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnOutputClassListStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnOutputClassListAction,
    props
  );
}

/**
 * A specialized hook for the FnOutputClassNested BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: TestClassNested
 * - Streaming Partial: RecursivePartialNull<TestClassNested>
 * - Streaming Final: TestClassNested
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnOutputClassNested();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to TestClassNested
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: TestClassNested | null
 *   partialData, // Type: RecursivePartialNull<TestClassNested> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnOutputClassNested({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnOutputClassNested<Action extends typeof ServerActions.FnOutputClassNestedAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnOutputClassNested<Action extends typeof ServerActions.FnOutputClassNestedAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnOutputClassNested<Action extends typeof ServerActions.FnOutputClassNestedAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnOutputClassNestedStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnOutputClassNestedAction,
    props
  );
}

/**
 * A specialized hook for the FnOutputClassWithEnum BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: TestClassWithEnum
 * - Streaming Partial: RecursivePartialNull<TestClassWithEnum>
 * - Streaming Final: TestClassWithEnum
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnOutputClassWithEnum();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to TestClassWithEnum
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: TestClassWithEnum | null
 *   partialData, // Type: RecursivePartialNull<TestClassWithEnum> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnOutputClassWithEnum({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnOutputClassWithEnum<Action extends typeof ServerActions.FnOutputClassWithEnumAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnOutputClassWithEnum<Action extends typeof ServerActions.FnOutputClassWithEnumAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnOutputClassWithEnum<Action extends typeof ServerActions.FnOutputClassWithEnumAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnOutputClassWithEnumStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnOutputClassWithEnumAction,
    props
  );
}

/**
 * A specialized hook for the FnOutputInt BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: number
 * - Streaming Partial: RecursivePartialNull<number>
 * - Streaming Final: number
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnOutputInt();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to number
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: number | null
 *   partialData, // Type: RecursivePartialNull<number> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnOutputInt({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnOutputInt<Action extends typeof ServerActions.FnOutputIntAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnOutputInt<Action extends typeof ServerActions.FnOutputIntAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnOutputInt<Action extends typeof ServerActions.FnOutputIntAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnOutputIntStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnOutputIntAction,
    props
  );
}

/**
 * A specialized hook for the FnOutputLiteralBool BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: false
 * - Streaming Partial: RecursivePartialNull<false>
 * - Streaming Final: false
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnOutputLiteralBool();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to false
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: false | null
 *   partialData, // Type: RecursivePartialNull<false> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnOutputLiteralBool({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnOutputLiteralBool<Action extends typeof ServerActions.FnOutputLiteralBoolAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnOutputLiteralBool<Action extends typeof ServerActions.FnOutputLiteralBoolAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnOutputLiteralBool<Action extends typeof ServerActions.FnOutputLiteralBoolAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnOutputLiteralBoolStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnOutputLiteralBoolAction,
    props
  );
}

/**
 * A specialized hook for the FnOutputLiteralInt BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: 5
 * - Streaming Partial: RecursivePartialNull<5>
 * - Streaming Final: 5
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnOutputLiteralInt();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to 5
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: 5 | null
 *   partialData, // Type: RecursivePartialNull<5> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnOutputLiteralInt({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnOutputLiteralInt<Action extends typeof ServerActions.FnOutputLiteralIntAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnOutputLiteralInt<Action extends typeof ServerActions.FnOutputLiteralIntAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnOutputLiteralInt<Action extends typeof ServerActions.FnOutputLiteralIntAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnOutputLiteralIntStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnOutputLiteralIntAction,
    props
  );
}

/**
 * A specialized hook for the FnOutputLiteralString BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: "example output"
 * - Streaming Partial: RecursivePartialNull<"example output">
 * - Streaming Final: "example output"
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnOutputLiteralString();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to "example output"
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: "example output" | null
 *   partialData, // Type: RecursivePartialNull<"example output"> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnOutputLiteralString({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnOutputLiteralString<Action extends typeof ServerActions.FnOutputLiteralStringAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnOutputLiteralString<Action extends typeof ServerActions.FnOutputLiteralStringAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnOutputLiteralString<Action extends typeof ServerActions.FnOutputLiteralStringAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnOutputLiteralStringStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnOutputLiteralStringAction,
    props
  );
}

/**
 * A specialized hook for the FnOutputStringList BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string[]
 * - Streaming Partial: RecursivePartialNull<string[]>
 * - Streaming Final: string[]
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnOutputStringList();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string[]
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string[] | null
 *   partialData, // Type: RecursivePartialNull<string[]> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnOutputStringList({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnOutputStringList<Action extends typeof ServerActions.FnOutputStringListAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnOutputStringList<Action extends typeof ServerActions.FnOutputStringListAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnOutputStringList<Action extends typeof ServerActions.FnOutputStringListAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnOutputStringListStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnOutputStringListAction,
    props
  );
}

/**
 * A specialized hook for the FnTestAliasedEnumOutput BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: TestEnum
 * - Streaming Partial: RecursivePartialNull<TestEnum>
 * - Streaming Final: TestEnum
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnTestAliasedEnumOutput();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to TestEnum
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: TestEnum | null
 *   partialData, // Type: RecursivePartialNull<TestEnum> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnTestAliasedEnumOutput({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnTestAliasedEnumOutput<Action extends typeof ServerActions.FnTestAliasedEnumOutputAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnTestAliasedEnumOutput<Action extends typeof ServerActions.FnTestAliasedEnumOutputAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnTestAliasedEnumOutput<Action extends typeof ServerActions.FnTestAliasedEnumOutputAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnTestAliasedEnumOutputStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnTestAliasedEnumOutputAction,
    props
  );
}

/**
 * A specialized hook for the FnTestClassAlias BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: TestClassAlias
 * - Streaming Partial: RecursivePartialNull<TestClassAlias>
 * - Streaming Final: TestClassAlias
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnTestClassAlias();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to TestClassAlias
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: TestClassAlias | null
 *   partialData, // Type: RecursivePartialNull<TestClassAlias> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnTestClassAlias({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnTestClassAlias<Action extends typeof ServerActions.FnTestClassAliasAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnTestClassAlias<Action extends typeof ServerActions.FnTestClassAliasAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnTestClassAlias<Action extends typeof ServerActions.FnTestClassAliasAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnTestClassAliasStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnTestClassAliasAction,
    props
  );
}

/**
 * A specialized hook for the FnTestNamedArgsSingleEnum BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myArg: types.NamedArgsSingleEnum
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useFnTestNamedArgsSingleEnum();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useFnTestNamedArgsSingleEnum({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myArg: someValue as NamedArgsSingleEnum,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myArg: firstValue as NamedArgsSingleEnum,
 *     }),
 *     mutate({
 *       myArg: secondValue as NamedArgsSingleEnum,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useFnTestNamedArgsSingleEnum<Action extends typeof ServerActions.FnTestNamedArgsSingleEnumAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useFnTestNamedArgsSingleEnum<Action extends typeof ServerActions.FnTestNamedArgsSingleEnumAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useFnTestNamedArgsSingleEnum<Action extends typeof ServerActions.FnTestNamedArgsSingleEnumAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.FnTestNamedArgsSingleEnumStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.FnTestNamedArgsSingleEnumAction,
    props
  );
}

/**
 * A specialized hook for the GetDataType BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - text: types.string
 *
 *
 * Return Type:
 * - Non-streaming: RaysData
 * - Streaming Partial: RecursivePartialNull<RaysData>
 * - Streaming Final: RaysData
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useGetDataType();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to RaysData
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: RaysData | null
 *   partialData, // Type: RecursivePartialNull<RaysData> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useGetDataType({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       text: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       text: firstValue as string,
 *     }),
 *     mutate({
 *       text: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useGetDataType<Action extends typeof ServerActions.GetDataTypeAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useGetDataType<Action extends typeof ServerActions.GetDataTypeAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useGetDataType<Action extends typeof ServerActions.GetDataTypeAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.GetDataTypeStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.GetDataTypeAction,
    props
  );
}

/**
 * A specialized hook for the GetOrderInfo BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - email: types.Email
 *
 *
 * Return Type:
 * - Non-streaming: OrderInfo
 * - Streaming Partial: RecursivePartialNull<OrderInfo>
 * - Streaming Final: OrderInfo
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useGetOrderInfo();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to OrderInfo
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: OrderInfo | null
 *   partialData, // Type: RecursivePartialNull<OrderInfo> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useGetOrderInfo({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       email: someValue as Email,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       email: firstValue as Email,
 *     }),
 *     mutate({
 *       email: secondValue as Email,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useGetOrderInfo<Action extends typeof ServerActions.GetOrderInfoAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useGetOrderInfo<Action extends typeof ServerActions.GetOrderInfoAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useGetOrderInfo<Action extends typeof ServerActions.GetOrderInfoAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.GetOrderInfoStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.GetOrderInfoAction,
    props
  );
}

/**
 * A specialized hook for the GetQuery BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - query: types.string
 *
 *
 * Return Type:
 * - Non-streaming: SearchParams
 * - Streaming Partial: RecursivePartialNull<SearchParams>
 * - Streaming Final: SearchParams
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useGetQuery();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to SearchParams
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: SearchParams | null
 *   partialData, // Type: RecursivePartialNull<SearchParams> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useGetQuery({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       query: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       query: firstValue as string,
 *     }),
 *     mutate({
 *       query: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useGetQuery<Action extends typeof ServerActions.GetQueryAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useGetQuery<Action extends typeof ServerActions.GetQueryAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useGetQuery<Action extends typeof ServerActions.GetQueryAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.GetQueryStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.GetQueryAction,
    props
  );
}

/**
 * A specialized hook for the InOutEnumMapKey BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - i1: types.Partial<Record<types.MapKey, string>>
 *
 * - i2: types.Partial<Record<types.MapKey, string>>
 *
 *
 * Return Type:
 * - Non-streaming: Partial<Record<types.MapKey, string>>
 * - Streaming Partial: RecursivePartialNull<Partial<Record<types.MapKey, string>>>
 * - Streaming Final: Partial<Record<types.MapKey, string>>
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useInOutEnumMapKey();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Partial<Record<types.MapKey, string>>
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Partial<Record<types.MapKey, string>> | null
 *   partialData, // Type: RecursivePartialNull<Partial<Record<types.MapKey, string>>> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useInOutEnumMapKey({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       i1: someValue as Partial<Record<types.MapKey, string>>,  // Replace someValue with your data
 *       i2: someValue as Partial<Record<types.MapKey, string>>,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       i1: firstValue as Partial<Record<types.MapKey, string>>,
 *       i2: firstValue as Partial<Record<types.MapKey, string>>,
 *     }),
 *     mutate({
 *       i1: secondValue as Partial<Record<types.MapKey, string>>,
 *       i2: secondValue as Partial<Record<types.MapKey, string>>,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useInOutEnumMapKey<Action extends typeof ServerActions.InOutEnumMapKeyAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useInOutEnumMapKey<Action extends typeof ServerActions.InOutEnumMapKeyAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useInOutEnumMapKey<Action extends typeof ServerActions.InOutEnumMapKeyAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.InOutEnumMapKeyStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.InOutEnumMapKeyAction,
    props
  );
}

/**
 * A specialized hook for the InOutLiteralStringUnionMapKey BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - i1: types.Partial<Record<"one" | "two" | "three" | "four", string>>
 *
 * - i2: types.Partial<Record<"one" | "two" | "three" | "four", string>>
 *
 *
 * Return Type:
 * - Non-streaming: Partial<Record<"one" | "two" | "three" | "four", string>>
 * - Streaming Partial: RecursivePartialNull<Partial<Record<"one" | "two" | "three" | "four", string>>>
 * - Streaming Final: Partial<Record<"one" | "two" | "three" | "four", string>>
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useInOutLiteralStringUnionMapKey();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Partial<Record<"one" | "two" | "three" | "four", string>>
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Partial<Record<"one" | "two" | "three" | "four", string>> | null
 *   partialData, // Type: RecursivePartialNull<Partial<Record<"one" | "two" | "three" | "four", string>>> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useInOutLiteralStringUnionMapKey({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       i1: someValue as Partial<Record<"one" | "two" | "three" | "four", string>>,  // Replace someValue with your data
 *       i2: someValue as Partial<Record<"one" | "two" | "three" | "four", string>>,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       i1: firstValue as Partial<Record<"one" | "two" | "three" | "four", string>>,
 *       i2: firstValue as Partial<Record<"one" | "two" | "three" | "four", string>>,
 *     }),
 *     mutate({
 *       i1: secondValue as Partial<Record<"one" | "two" | "three" | "four", string>>,
 *       i2: secondValue as Partial<Record<"one" | "two" | "three" | "four", string>>,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useInOutLiteralStringUnionMapKey<Action extends typeof ServerActions.InOutLiteralStringUnionMapKeyAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useInOutLiteralStringUnionMapKey<Action extends typeof ServerActions.InOutLiteralStringUnionMapKeyAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useInOutLiteralStringUnionMapKey<Action extends typeof ServerActions.InOutLiteralStringUnionMapKeyAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.InOutLiteralStringUnionMapKeyStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.InOutLiteralStringUnionMapKeyAction,
    props
  );
}

/**
 * A specialized hook for the InOutSingleLiteralStringMapKey BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - m: types.Partial<Record<"key", string>>
 *
 *
 * Return Type:
 * - Non-streaming: Partial<Record<"key", string>>
 * - Streaming Partial: RecursivePartialNull<Partial<Record<"key", string>>>
 * - Streaming Final: Partial<Record<"key", string>>
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useInOutSingleLiteralStringMapKey();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Partial<Record<"key", string>>
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Partial<Record<"key", string>> | null
 *   partialData, // Type: RecursivePartialNull<Partial<Record<"key", string>>> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useInOutSingleLiteralStringMapKey({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       m: someValue as Partial<Record<"key", string>>,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       m: firstValue as Partial<Record<"key", string>>,
 *     }),
 *     mutate({
 *       m: secondValue as Partial<Record<"key", string>>,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useInOutSingleLiteralStringMapKey<Action extends typeof ServerActions.InOutSingleLiteralStringMapKeyAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useInOutSingleLiteralStringMapKey<Action extends typeof ServerActions.InOutSingleLiteralStringMapKeyAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useInOutSingleLiteralStringMapKey<Action extends typeof ServerActions.InOutSingleLiteralStringMapKeyAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.InOutSingleLiteralStringMapKeyStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.InOutSingleLiteralStringMapKeyAction,
    props
  );
}

/**
 * A specialized hook for the JsonTypeAliasCycle BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.JsonValue
 *
 *
 * Return Type:
 * - Non-streaming: JsonValue
 * - Streaming Partial: RecursivePartialNull<JsonValue>
 * - Streaming Final: JsonValue
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useJsonTypeAliasCycle();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to JsonValue
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: JsonValue | null
 *   partialData, // Type: RecursivePartialNull<JsonValue> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useJsonTypeAliasCycle({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as JsonValue,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as JsonValue,
 *     }),
 *     mutate({
 *       input: secondValue as JsonValue,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useJsonTypeAliasCycle<Action extends typeof ServerActions.JsonTypeAliasCycleAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useJsonTypeAliasCycle<Action extends typeof ServerActions.JsonTypeAliasCycleAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useJsonTypeAliasCycle<Action extends typeof ServerActions.JsonTypeAliasCycleAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.JsonTypeAliasCycleStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.JsonTypeAliasCycleAction,
    props
  );
}

/**
 * A specialized hook for the LiteralUnionsTest BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: 1 | true | "string output"
 * - Streaming Partial: RecursivePartialNull<1 | true | "string output">
 * - Streaming Final: 1 | true | "string output"
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useLiteralUnionsTest();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to 1 | true | "string output"
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: 1 | true | "string output" | null
 *   partialData, // Type: RecursivePartialNull<1 | true | "string output"> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useLiteralUnionsTest({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useLiteralUnionsTest<Action extends typeof ServerActions.LiteralUnionsTestAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useLiteralUnionsTest<Action extends typeof ServerActions.LiteralUnionsTestAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useLiteralUnionsTest<Action extends typeof ServerActions.LiteralUnionsTestAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.LiteralUnionsTestStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.LiteralUnionsTestAction,
    props
  );
}

/**
 * A specialized hook for the MakeBlockConstraint BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 *
 * Return Type:
 * - Non-streaming: Checked<BlockConstraint,"cross_field">
 * - Streaming Partial: RecursivePartialNull<Checked<BlockConstraint,"cross_field">>
 * - Streaming Final: Checked<BlockConstraint,"cross_field">
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useMakeBlockConstraint();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Checked<BlockConstraint,"cross_field">
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Checked<BlockConstraint,"cross_field"> | null
 *   partialData, // Type: RecursivePartialNull<Checked<BlockConstraint,"cross_field">> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useMakeBlockConstraint({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *     }),
 *     mutate({
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useMakeBlockConstraint<Action extends typeof ServerActions.MakeBlockConstraintAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useMakeBlockConstraint<Action extends typeof ServerActions.MakeBlockConstraintAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useMakeBlockConstraint<Action extends typeof ServerActions.MakeBlockConstraintAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.MakeBlockConstraintStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.MakeBlockConstraintAction,
    props
  );
}

/**
 * A specialized hook for the MakeNestedBlockConstraint BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 *
 * Return Type:
 * - Non-streaming: NestedBlockConstraint
 * - Streaming Partial: RecursivePartialNull<NestedBlockConstraint>
 * - Streaming Final: NestedBlockConstraint
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useMakeNestedBlockConstraint();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to NestedBlockConstraint
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: NestedBlockConstraint | null
 *   partialData, // Type: RecursivePartialNull<NestedBlockConstraint> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useMakeNestedBlockConstraint({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *     }),
 *     mutate({
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useMakeNestedBlockConstraint<Action extends typeof ServerActions.MakeNestedBlockConstraintAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useMakeNestedBlockConstraint<Action extends typeof ServerActions.MakeNestedBlockConstraintAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useMakeNestedBlockConstraint<Action extends typeof ServerActions.MakeNestedBlockConstraintAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.MakeNestedBlockConstraintStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.MakeNestedBlockConstraintAction,
    props
  );
}

/**
 * A specialized hook for the MakeSemanticContainer BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 *
 * Return Type:
 * - Non-streaming: SemanticContainer
 * - Streaming Partial: RecursivePartialNull<SemanticContainer>
 * - Streaming Final: SemanticContainer
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useMakeSemanticContainer();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to SemanticContainer
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: SemanticContainer | null
 *   partialData, // Type: RecursivePartialNull<SemanticContainer> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useMakeSemanticContainer({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *     }),
 *     mutate({
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useMakeSemanticContainer<Action extends typeof ServerActions.MakeSemanticContainerAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useMakeSemanticContainer<Action extends typeof ServerActions.MakeSemanticContainerAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useMakeSemanticContainer<Action extends typeof ServerActions.MakeSemanticContainerAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.MakeSemanticContainerStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.MakeSemanticContainerAction,
    props
  );
}

/**
 * A specialized hook for the MapAlias BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - m: types.Record<string, string[]>
 *
 *
 * Return Type:
 * - Non-streaming: Record<string, string[]>
 * - Streaming Partial: RecursivePartialNull<Record<string, string[]>>
 * - Streaming Final: Record<string, string[]>
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useMapAlias();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Record<string, string[]>
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Record<string, string[]> | null
 *   partialData, // Type: RecursivePartialNull<Record<string, string[]>> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useMapAlias({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       m: someValue as Record<string, string[]>,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       m: firstValue as Record<string, string[]>,
 *     }),
 *     mutate({
 *       m: secondValue as Record<string, string[]>,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useMapAlias<Action extends typeof ServerActions.MapAliasAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useMapAlias<Action extends typeof ServerActions.MapAliasAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useMapAlias<Action extends typeof ServerActions.MapAliasAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.MapAliasStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.MapAliasAction,
    props
  );
}

/**
 * A specialized hook for the MergeAliasAttributes BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - money: types.number
 *
 *
 * Return Type:
 * - Non-streaming: MergeAttrs
 * - Streaming Partial: RecursivePartialNull<MergeAttrs>
 * - Streaming Final: MergeAttrs
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useMergeAliasAttributes();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to MergeAttrs
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: MergeAttrs | null
 *   partialData, // Type: RecursivePartialNull<MergeAttrs> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useMergeAliasAttributes({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       money: someValue as number,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       money: firstValue as number,
 *     }),
 *     mutate({
 *       money: secondValue as number,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useMergeAliasAttributes<Action extends typeof ServerActions.MergeAliasAttributesAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useMergeAliasAttributes<Action extends typeof ServerActions.MergeAliasAttributesAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useMergeAliasAttributes<Action extends typeof ServerActions.MergeAliasAttributesAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.MergeAliasAttributesStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.MergeAliasAttributesAction,
    props
  );
}

/**
 * A specialized hook for the MyFunc BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: DynamicOutput
 * - Streaming Partial: RecursivePartialNull<DynamicOutput>
 * - Streaming Final: DynamicOutput
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useMyFunc();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to DynamicOutput
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: DynamicOutput | null
 *   partialData, // Type: RecursivePartialNull<DynamicOutput> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useMyFunc({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useMyFunc<Action extends typeof ServerActions.MyFuncAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useMyFunc<Action extends typeof ServerActions.MyFuncAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useMyFunc<Action extends typeof ServerActions.MyFuncAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.MyFuncStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.MyFuncAction,
    props
  );
}

/**
 * A specialized hook for the NestedAlias BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - c: types.number | string | boolean | number | string[] | Record<string, string[]>
 *
 *
 * Return Type:
 * - Non-streaming: number | string | boolean | number | string[] | Record<string, string[]>
 * - Streaming Partial: RecursivePartialNull<number | string | boolean | number | string[] | Record<string, string[]>>
 * - Streaming Final: number | string | boolean | number | string[] | Record<string, string[]>
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useNestedAlias();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to number | string | boolean | number | string[] | Record<string, string[]>
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: number | string | boolean | number | string[] | Record<string, string[]> | null
 *   partialData, // Type: RecursivePartialNull<number | string | boolean | number | string[] | Record<string, string[]>> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useNestedAlias({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       c: someValue as number | string | boolean | number | string[] | Record<string, string[]>,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       c: firstValue as number | string | boolean | number | string[] | Record<string, string[]>,
 *     }),
 *     mutate({
 *       c: secondValue as number | string | boolean | number | string[] | Record<string, string[]>,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useNestedAlias<Action extends typeof ServerActions.NestedAliasAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useNestedAlias<Action extends typeof ServerActions.NestedAliasAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useNestedAlias<Action extends typeof ServerActions.NestedAliasAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.NestedAliasStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.NestedAliasAction,
    props
  );
}

/**
 * A specialized hook for the NullLiteralClassHello BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - s: types.string
 *
 *
 * Return Type:
 * - Non-streaming: ClassForNullLiteral
 * - Streaming Partial: RecursivePartialNull<ClassForNullLiteral>
 * - Streaming Final: ClassForNullLiteral
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useNullLiteralClassHello();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to ClassForNullLiteral
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: ClassForNullLiteral | null
 *   partialData, // Type: RecursivePartialNull<ClassForNullLiteral> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useNullLiteralClassHello({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       s: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       s: firstValue as string,
 *     }),
 *     mutate({
 *       s: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useNullLiteralClassHello<Action extends typeof ServerActions.NullLiteralClassHelloAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useNullLiteralClassHello<Action extends typeof ServerActions.NullLiteralClassHelloAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useNullLiteralClassHello<Action extends typeof ServerActions.NullLiteralClassHelloAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.NullLiteralClassHelloStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.NullLiteralClassHelloAction,
    props
  );
}

/**
 * A specialized hook for the OptionalTest_Function BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: (OptionalTest_ReturnType | null)[]
 * - Streaming Partial: RecursivePartialNull<(OptionalTest_ReturnType | null)[]>
 * - Streaming Final: (OptionalTest_ReturnType | null)[]
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useOptionalTest_Function();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to (OptionalTest_ReturnType | null)[]
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: (OptionalTest_ReturnType | null)[] | null
 *   partialData, // Type: RecursivePartialNull<(OptionalTest_ReturnType | null)[]> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useOptionalTest_Function({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useOptionalTest_Function<Action extends typeof ServerActions.OptionalTest_FunctionAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useOptionalTest_Function<Action extends typeof ServerActions.OptionalTest_FunctionAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useOptionalTest_Function<Action extends typeof ServerActions.OptionalTest_FunctionAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.OptionalTest_FunctionStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.OptionalTest_FunctionAction,
    props
  );
}

/**
 * A specialized hook for the PredictAge BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - name: types.string
 *
 *
 * Return Type:
 * - Non-streaming: FooAny
 * - Streaming Partial: RecursivePartialNull<FooAny>
 * - Streaming Final: FooAny
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = usePredictAge();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to FooAny
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: FooAny | null
 *   partialData, // Type: RecursivePartialNull<FooAny> | null
 *   isPending,
 *   error,
 *   mutate
 * } = usePredictAge({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       name: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       name: firstValue as string,
 *     }),
 *     mutate({
 *       name: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function usePredictAge<Action extends typeof ServerActions.PredictAgeAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function usePredictAge<Action extends typeof ServerActions.PredictAgeAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function usePredictAge<Action extends typeof ServerActions.PredictAgeAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.PredictAgeStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.PredictAgeAction,
    props
  );
}

/**
 * A specialized hook for the PredictAgeBare BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - inp: types.string
 *
 *
 * Return Type:
 * - Non-streaming: Checked<number,"too_big">
 * - Streaming Partial: RecursivePartialNull<Checked<number,"too_big">>
 * - Streaming Final: Checked<number,"too_big">
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = usePredictAgeBare();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Checked<number,"too_big">
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Checked<number,"too_big"> | null
 *   partialData, // Type: RecursivePartialNull<Checked<number,"too_big">> | null
 *   isPending,
 *   error,
 *   mutate
 * } = usePredictAgeBare({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       inp: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       inp: firstValue as string,
 *     }),
 *     mutate({
 *       inp: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function usePredictAgeBare<Action extends typeof ServerActions.PredictAgeBareAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function usePredictAgeBare<Action extends typeof ServerActions.PredictAgeBareAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function usePredictAgeBare<Action extends typeof ServerActions.PredictAgeBareAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.PredictAgeBareStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.PredictAgeBareAction,
    props
  );
}

/**
 * A specialized hook for the PrimitiveAlias BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - p: types.number | string | boolean | number
 *
 *
 * Return Type:
 * - Non-streaming: number | string | boolean | number
 * - Streaming Partial: RecursivePartialNull<number | string | boolean | number>
 * - Streaming Final: number | string | boolean | number
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = usePrimitiveAlias();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to number | string | boolean | number
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: number | string | boolean | number | null
 *   partialData, // Type: RecursivePartialNull<number | string | boolean | number> | null
 *   isPending,
 *   error,
 *   mutate
 * } = usePrimitiveAlias({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       p: someValue as number | string | boolean | number,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       p: firstValue as number | string | boolean | number,
 *     }),
 *     mutate({
 *       p: secondValue as number | string | boolean | number,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function usePrimitiveAlias<Action extends typeof ServerActions.PrimitiveAliasAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function usePrimitiveAlias<Action extends typeof ServerActions.PrimitiveAliasAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function usePrimitiveAlias<Action extends typeof ServerActions.PrimitiveAliasAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.PrimitiveAliasStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.PrimitiveAliasAction,
    props
  );
}

/**
 * A specialized hook for the PromptTestClaude BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = usePromptTestClaude();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = usePromptTestClaude({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function usePromptTestClaude<Action extends typeof ServerActions.PromptTestClaudeAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function usePromptTestClaude<Action extends typeof ServerActions.PromptTestClaudeAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function usePromptTestClaude<Action extends typeof ServerActions.PromptTestClaudeAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.PromptTestClaudeStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.PromptTestClaudeAction,
    props
  );
}

/**
 * A specialized hook for the PromptTestClaudeChat BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = usePromptTestClaudeChat();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = usePromptTestClaudeChat({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function usePromptTestClaudeChat<Action extends typeof ServerActions.PromptTestClaudeChatAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function usePromptTestClaudeChat<Action extends typeof ServerActions.PromptTestClaudeChatAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function usePromptTestClaudeChat<Action extends typeof ServerActions.PromptTestClaudeChatAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.PromptTestClaudeChatStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.PromptTestClaudeChatAction,
    props
  );
}

/**
 * A specialized hook for the PromptTestClaudeChatNoSystem BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = usePromptTestClaudeChatNoSystem();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = usePromptTestClaudeChatNoSystem({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function usePromptTestClaudeChatNoSystem<Action extends typeof ServerActions.PromptTestClaudeChatNoSystemAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function usePromptTestClaudeChatNoSystem<Action extends typeof ServerActions.PromptTestClaudeChatNoSystemAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function usePromptTestClaudeChatNoSystem<Action extends typeof ServerActions.PromptTestClaudeChatNoSystemAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.PromptTestClaudeChatNoSystemStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.PromptTestClaudeChatNoSystemAction,
    props
  );
}

/**
 * A specialized hook for the PromptTestOpenAI BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = usePromptTestOpenAI();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = usePromptTestOpenAI({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function usePromptTestOpenAI<Action extends typeof ServerActions.PromptTestOpenAIAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function usePromptTestOpenAI<Action extends typeof ServerActions.PromptTestOpenAIAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function usePromptTestOpenAI<Action extends typeof ServerActions.PromptTestOpenAIAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.PromptTestOpenAIStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.PromptTestOpenAIAction,
    props
  );
}

/**
 * A specialized hook for the PromptTestOpenAIChat BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = usePromptTestOpenAIChat();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = usePromptTestOpenAIChat({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function usePromptTestOpenAIChat<Action extends typeof ServerActions.PromptTestOpenAIChatAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function usePromptTestOpenAIChat<Action extends typeof ServerActions.PromptTestOpenAIChatAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function usePromptTestOpenAIChat<Action extends typeof ServerActions.PromptTestOpenAIChatAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.PromptTestOpenAIChatStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.PromptTestOpenAIChatAction,
    props
  );
}

/**
 * A specialized hook for the PromptTestOpenAIChatNoSystem BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = usePromptTestOpenAIChatNoSystem();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = usePromptTestOpenAIChatNoSystem({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function usePromptTestOpenAIChatNoSystem<Action extends typeof ServerActions.PromptTestOpenAIChatNoSystemAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function usePromptTestOpenAIChatNoSystem<Action extends typeof ServerActions.PromptTestOpenAIChatNoSystemAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function usePromptTestOpenAIChatNoSystem<Action extends typeof ServerActions.PromptTestOpenAIChatNoSystemAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.PromptTestOpenAIChatNoSystemStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.PromptTestOpenAIChatNoSystemAction,
    props
  );
}

/**
 * A specialized hook for the PromptTestStreaming BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = usePromptTestStreaming();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = usePromptTestStreaming({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function usePromptTestStreaming<Action extends typeof ServerActions.PromptTestStreamingAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function usePromptTestStreaming<Action extends typeof ServerActions.PromptTestStreamingAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function usePromptTestStreaming<Action extends typeof ServerActions.PromptTestStreamingAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.PromptTestStreamingStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.PromptTestStreamingAction,
    props
  );
}

/**
 * A specialized hook for the RecursiveAliasCycle BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.RecAliasOne
 *
 *
 * Return Type:
 * - Non-streaming: RecAliasOne
 * - Streaming Partial: RecursivePartialNull<RecAliasOne>
 * - Streaming Final: RecAliasOne
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useRecursiveAliasCycle();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to RecAliasOne
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: RecAliasOne | null
 *   partialData, // Type: RecursivePartialNull<RecAliasOne> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useRecursiveAliasCycle({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as RecAliasOne,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as RecAliasOne,
 *     }),
 *     mutate({
 *       input: secondValue as RecAliasOne,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useRecursiveAliasCycle<Action extends typeof ServerActions.RecursiveAliasCycleAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useRecursiveAliasCycle<Action extends typeof ServerActions.RecursiveAliasCycleAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useRecursiveAliasCycle<Action extends typeof ServerActions.RecursiveAliasCycleAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.RecursiveAliasCycleStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.RecursiveAliasCycleAction,
    props
  );
}

/**
 * A specialized hook for the RecursiveClassWithAliasIndirection BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - cls: types.NodeWithAliasIndirection
 *
 *
 * Return Type:
 * - Non-streaming: NodeWithAliasIndirection
 * - Streaming Partial: RecursivePartialNull<NodeWithAliasIndirection>
 * - Streaming Final: NodeWithAliasIndirection
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useRecursiveClassWithAliasIndirection();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to NodeWithAliasIndirection
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: NodeWithAliasIndirection | null
 *   partialData, // Type: RecursivePartialNull<NodeWithAliasIndirection> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useRecursiveClassWithAliasIndirection({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       cls: someValue as NodeWithAliasIndirection,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       cls: firstValue as NodeWithAliasIndirection,
 *     }),
 *     mutate({
 *       cls: secondValue as NodeWithAliasIndirection,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useRecursiveClassWithAliasIndirection<Action extends typeof ServerActions.RecursiveClassWithAliasIndirectionAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useRecursiveClassWithAliasIndirection<Action extends typeof ServerActions.RecursiveClassWithAliasIndirectionAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useRecursiveClassWithAliasIndirection<Action extends typeof ServerActions.RecursiveClassWithAliasIndirectionAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.RecursiveClassWithAliasIndirectionStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.RecursiveClassWithAliasIndirectionAction,
    props
  );
}

/**
 * A specialized hook for the ReturnAliasWithMergedAttributes BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - money: types.Checked<number,"gt_ten">
 *
 *
 * Return Type:
 * - Non-streaming: Checked<number,"gt_ten">
 * - Streaming Partial: RecursivePartialNull<Checked<number,"gt_ten">>
 * - Streaming Final: Checked<number,"gt_ten">
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useReturnAliasWithMergedAttributes();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Checked<number,"gt_ten">
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Checked<number,"gt_ten"> | null
 *   partialData, // Type: RecursivePartialNull<Checked<number,"gt_ten">> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useReturnAliasWithMergedAttributes({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       money: someValue as Checked<number,"gt_ten">,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       money: firstValue as Checked<number,"gt_ten">,
 *     }),
 *     mutate({
 *       money: secondValue as Checked<number,"gt_ten">,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useReturnAliasWithMergedAttributes<Action extends typeof ServerActions.ReturnAliasWithMergedAttributesAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useReturnAliasWithMergedAttributes<Action extends typeof ServerActions.ReturnAliasWithMergedAttributesAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useReturnAliasWithMergedAttributes<Action extends typeof ServerActions.ReturnAliasWithMergedAttributesAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ReturnAliasWithMergedAttributesStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ReturnAliasWithMergedAttributesAction,
    props
  );
}

/**
 * A specialized hook for the ReturnFailingAssert BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - inp: types.number
 *
 *
 * Return Type:
 * - Non-streaming: number
 * - Streaming Partial: RecursivePartialNull<number>
 * - Streaming Final: number
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useReturnFailingAssert();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to number
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: number | null
 *   partialData, // Type: RecursivePartialNull<number> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useReturnFailingAssert({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       inp: someValue as number,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       inp: firstValue as number,
 *     }),
 *     mutate({
 *       inp: secondValue as number,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useReturnFailingAssert<Action extends typeof ServerActions.ReturnFailingAssertAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useReturnFailingAssert<Action extends typeof ServerActions.ReturnFailingAssertAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useReturnFailingAssert<Action extends typeof ServerActions.ReturnFailingAssertAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ReturnFailingAssertStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ReturnFailingAssertAction,
    props
  );
}

/**
 * A specialized hook for the ReturnMalformedConstraints BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - a: types.number
 *
 *
 * Return Type:
 * - Non-streaming: MalformedConstraints
 * - Streaming Partial: RecursivePartialNull<MalformedConstraints>
 * - Streaming Final: MalformedConstraints
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useReturnMalformedConstraints();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to MalformedConstraints
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: MalformedConstraints | null
 *   partialData, // Type: RecursivePartialNull<MalformedConstraints> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useReturnMalformedConstraints({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       a: someValue as number,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       a: firstValue as number,
 *     }),
 *     mutate({
 *       a: secondValue as number,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useReturnMalformedConstraints<Action extends typeof ServerActions.ReturnMalformedConstraintsAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useReturnMalformedConstraints<Action extends typeof ServerActions.ReturnMalformedConstraintsAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useReturnMalformedConstraints<Action extends typeof ServerActions.ReturnMalformedConstraintsAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.ReturnMalformedConstraintsStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.ReturnMalformedConstraintsAction,
    props
  );
}

/**
 * A specialized hook for the SchemaDescriptions BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: Schema
 * - Streaming Partial: RecursivePartialNull<Schema>
 * - Streaming Final: Schema
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useSchemaDescriptions();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Schema
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Schema | null
 *   partialData, // Type: RecursivePartialNull<Schema> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useSchemaDescriptions({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useSchemaDescriptions<Action extends typeof ServerActions.SchemaDescriptionsAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useSchemaDescriptions<Action extends typeof ServerActions.SchemaDescriptionsAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useSchemaDescriptions<Action extends typeof ServerActions.SchemaDescriptionsAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.SchemaDescriptionsStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.SchemaDescriptionsAction,
    props
  );
}

/**
 * A specialized hook for the SimpleRecursiveListAlias BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.RecursiveListAlias
 *
 *
 * Return Type:
 * - Non-streaming: RecursiveListAlias
 * - Streaming Partial: RecursivePartialNull<RecursiveListAlias>
 * - Streaming Final: RecursiveListAlias
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useSimpleRecursiveListAlias();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to RecursiveListAlias
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: RecursiveListAlias | null
 *   partialData, // Type: RecursivePartialNull<RecursiveListAlias> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useSimpleRecursiveListAlias({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as RecursiveListAlias,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as RecursiveListAlias,
 *     }),
 *     mutate({
 *       input: secondValue as RecursiveListAlias,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useSimpleRecursiveListAlias<Action extends typeof ServerActions.SimpleRecursiveListAliasAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useSimpleRecursiveListAlias<Action extends typeof ServerActions.SimpleRecursiveListAliasAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useSimpleRecursiveListAlias<Action extends typeof ServerActions.SimpleRecursiveListAliasAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.SimpleRecursiveListAliasStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.SimpleRecursiveListAliasAction,
    props
  );
}

/**
 * A specialized hook for the SimpleRecursiveMapAlias BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.RecursiveMapAlias
 *
 *
 * Return Type:
 * - Non-streaming: RecursiveMapAlias
 * - Streaming Partial: RecursivePartialNull<RecursiveMapAlias>
 * - Streaming Final: RecursiveMapAlias
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useSimpleRecursiveMapAlias();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to RecursiveMapAlias
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: RecursiveMapAlias | null
 *   partialData, // Type: RecursivePartialNull<RecursiveMapAlias> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useSimpleRecursiveMapAlias({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as RecursiveMapAlias,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as RecursiveMapAlias,
 *     }),
 *     mutate({
 *       input: secondValue as RecursiveMapAlias,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useSimpleRecursiveMapAlias<Action extends typeof ServerActions.SimpleRecursiveMapAliasAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useSimpleRecursiveMapAlias<Action extends typeof ServerActions.SimpleRecursiveMapAliasAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useSimpleRecursiveMapAlias<Action extends typeof ServerActions.SimpleRecursiveMapAliasAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.SimpleRecursiveMapAliasStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.SimpleRecursiveMapAliasAction,
    props
  );
}

/**
 * A specialized hook for the StreamBigNumbers BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - digits: types.number
 *
 *
 * Return Type:
 * - Non-streaming: BigNumbers
 * - Streaming Partial: RecursivePartialNull<BigNumbers>
 * - Streaming Final: BigNumbers
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useStreamBigNumbers();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to BigNumbers
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: BigNumbers | null
 *   partialData, // Type: RecursivePartialNull<BigNumbers> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useStreamBigNumbers({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       digits: someValue as number,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       digits: firstValue as number,
 *     }),
 *     mutate({
 *       digits: secondValue as number,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useStreamBigNumbers<Action extends typeof ServerActions.StreamBigNumbersAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useStreamBigNumbers<Action extends typeof ServerActions.StreamBigNumbersAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useStreamBigNumbers<Action extends typeof ServerActions.StreamBigNumbersAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.StreamBigNumbersStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.StreamBigNumbersAction,
    props
  );
}

/**
 * A specialized hook for the StreamFailingAssertion BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - theme: types.string
 *
 * - length: types.number
 *
 *
 * Return Type:
 * - Non-streaming: TwoStoriesOneTitle
 * - Streaming Partial: RecursivePartialNull<TwoStoriesOneTitle>
 * - Streaming Final: TwoStoriesOneTitle
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useStreamFailingAssertion();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to TwoStoriesOneTitle
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: TwoStoriesOneTitle | null
 *   partialData, // Type: RecursivePartialNull<TwoStoriesOneTitle> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useStreamFailingAssertion({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       theme: someValue as string,  // Replace someValue with your data
 *       length: someValue as number,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       theme: firstValue as string,
 *       length: firstValue as number,
 *     }),
 *     mutate({
 *       theme: secondValue as string,
 *       length: secondValue as number,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useStreamFailingAssertion<Action extends typeof ServerActions.StreamFailingAssertionAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useStreamFailingAssertion<Action extends typeof ServerActions.StreamFailingAssertionAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useStreamFailingAssertion<Action extends typeof ServerActions.StreamFailingAssertionAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.StreamFailingAssertionStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.StreamFailingAssertionAction,
    props
  );
}

/**
 * A specialized hook for the StreamOneBigNumber BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - digits: types.number
 *
 *
 * Return Type:
 * - Non-streaming: number
 * - Streaming Partial: RecursivePartialNull<number>
 * - Streaming Final: number
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useStreamOneBigNumber();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to number
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: number | null
 *   partialData, // Type: RecursivePartialNull<number> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useStreamOneBigNumber({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       digits: someValue as number,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       digits: firstValue as number,
 *     }),
 *     mutate({
 *       digits: secondValue as number,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useStreamOneBigNumber<Action extends typeof ServerActions.StreamOneBigNumberAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useStreamOneBigNumber<Action extends typeof ServerActions.StreamOneBigNumberAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useStreamOneBigNumber<Action extends typeof ServerActions.StreamOneBigNumberAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.StreamOneBigNumberStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.StreamOneBigNumberAction,
    props
  );
}

/**
 * A specialized hook for the StreamUnionIntegers BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - digits: types.number
 *
 *
 * Return Type:
 * - Non-streaming: (number | string)[]
 * - Streaming Partial: RecursivePartialNull<(number | string)[]>
 * - Streaming Final: (number | string)[]
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useStreamUnionIntegers();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to (number | string)[]
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: (number | string)[] | null
 *   partialData, // Type: RecursivePartialNull<(number | string)[]> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useStreamUnionIntegers({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       digits: someValue as number,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       digits: firstValue as number,
 *     }),
 *     mutate({
 *       digits: secondValue as number,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useStreamUnionIntegers<Action extends typeof ServerActions.StreamUnionIntegersAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useStreamUnionIntegers<Action extends typeof ServerActions.StreamUnionIntegersAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useStreamUnionIntegers<Action extends typeof ServerActions.StreamUnionIntegersAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.StreamUnionIntegersStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.StreamUnionIntegersAction,
    props
  );
}

/**
 * A specialized hook for the StreamingCompoundNumbers BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - digits: types.number
 *
 * - yapping: types.boolean
 *
 *
 * Return Type:
 * - Non-streaming: CompoundBigNumbers
 * - Streaming Partial: RecursivePartialNull<CompoundBigNumbers>
 * - Streaming Final: CompoundBigNumbers
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useStreamingCompoundNumbers();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to CompoundBigNumbers
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: CompoundBigNumbers | null
 *   partialData, // Type: RecursivePartialNull<CompoundBigNumbers> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useStreamingCompoundNumbers({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       digits: someValue as number,  // Replace someValue with your data
 *       yapping: someValue as boolean,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       digits: firstValue as number,
 *       yapping: firstValue as boolean,
 *     }),
 *     mutate({
 *       digits: secondValue as number,
 *       yapping: secondValue as boolean,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useStreamingCompoundNumbers<Action extends typeof ServerActions.StreamingCompoundNumbersAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useStreamingCompoundNumbers<Action extends typeof ServerActions.StreamingCompoundNumbersAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useStreamingCompoundNumbers<Action extends typeof ServerActions.StreamingCompoundNumbersAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.StreamingCompoundNumbersStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.StreamingCompoundNumbersAction,
    props
  );
}

/**
 * A specialized hook for the TakeRecAliasDep BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.RecursiveAliasDependency
 *
 *
 * Return Type:
 * - Non-streaming: RecursiveAliasDependency
 * - Streaming Partial: RecursivePartialNull<RecursiveAliasDependency>
 * - Streaming Final: RecursiveAliasDependency
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTakeRecAliasDep();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to RecursiveAliasDependency
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: RecursiveAliasDependency | null
 *   partialData, // Type: RecursivePartialNull<RecursiveAliasDependency> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTakeRecAliasDep({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as RecursiveAliasDependency,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as RecursiveAliasDependency,
 *     }),
 *     mutate({
 *       input: secondValue as RecursiveAliasDependency,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTakeRecAliasDep<Action extends typeof ServerActions.TakeRecAliasDepAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTakeRecAliasDep<Action extends typeof ServerActions.TakeRecAliasDepAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTakeRecAliasDep<Action extends typeof ServerActions.TakeRecAliasDepAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TakeRecAliasDepStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TakeRecAliasDepAction,
    props
  );
}

/**
 * A specialized hook for the TestAnthropic BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestAnthropic();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestAnthropic({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestAnthropic<Action extends typeof ServerActions.TestAnthropicAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestAnthropic<Action extends typeof ServerActions.TestAnthropicAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestAnthropic<Action extends typeof ServerActions.TestAnthropicAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestAnthropicStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestAnthropicAction,
    props
  );
}

/**
 * A specialized hook for the TestAnthropicShorthand BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestAnthropicShorthand();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestAnthropicShorthand({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestAnthropicShorthand<Action extends typeof ServerActions.TestAnthropicShorthandAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestAnthropicShorthand<Action extends typeof ServerActions.TestAnthropicShorthandAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestAnthropicShorthand<Action extends typeof ServerActions.TestAnthropicShorthandAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestAnthropicShorthandStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestAnthropicShorthandAction,
    props
  );
}

/**
 * A specialized hook for the TestAws BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestAws();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestAws({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestAws<Action extends typeof ServerActions.TestAwsAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestAws<Action extends typeof ServerActions.TestAwsAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestAws<Action extends typeof ServerActions.TestAwsAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestAwsStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestAwsAction,
    props
  );
}

/**
 * A specialized hook for the TestAwsInvalidAccessKey BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestAwsInvalidAccessKey();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestAwsInvalidAccessKey({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestAwsInvalidAccessKey<Action extends typeof ServerActions.TestAwsInvalidAccessKeyAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestAwsInvalidAccessKey<Action extends typeof ServerActions.TestAwsInvalidAccessKeyAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestAwsInvalidAccessKey<Action extends typeof ServerActions.TestAwsInvalidAccessKeyAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestAwsInvalidAccessKeyStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestAwsInvalidAccessKeyAction,
    props
  );
}

/**
 * A specialized hook for the TestAwsInvalidProfile BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestAwsInvalidProfile();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestAwsInvalidProfile({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestAwsInvalidProfile<Action extends typeof ServerActions.TestAwsInvalidProfileAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestAwsInvalidProfile<Action extends typeof ServerActions.TestAwsInvalidProfileAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestAwsInvalidProfile<Action extends typeof ServerActions.TestAwsInvalidProfileAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestAwsInvalidProfileStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestAwsInvalidProfileAction,
    props
  );
}

/**
 * A specialized hook for the TestAwsInvalidRegion BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestAwsInvalidRegion();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestAwsInvalidRegion({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestAwsInvalidRegion<Action extends typeof ServerActions.TestAwsInvalidRegionAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestAwsInvalidRegion<Action extends typeof ServerActions.TestAwsInvalidRegionAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestAwsInvalidRegion<Action extends typeof ServerActions.TestAwsInvalidRegionAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestAwsInvalidRegionStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestAwsInvalidRegionAction,
    props
  );
}

/**
 * A specialized hook for the TestAwsInvalidSessionToken BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestAwsInvalidSessionToken();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestAwsInvalidSessionToken({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestAwsInvalidSessionToken<Action extends typeof ServerActions.TestAwsInvalidSessionTokenAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestAwsInvalidSessionToken<Action extends typeof ServerActions.TestAwsInvalidSessionTokenAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestAwsInvalidSessionToken<Action extends typeof ServerActions.TestAwsInvalidSessionTokenAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestAwsInvalidSessionTokenStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestAwsInvalidSessionTokenAction,
    props
  );
}

/**
 * A specialized hook for the TestAzure BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestAzure();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestAzure({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestAzure<Action extends typeof ServerActions.TestAzureAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestAzure<Action extends typeof ServerActions.TestAzureAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestAzure<Action extends typeof ServerActions.TestAzureAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestAzureStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestAzureAction,
    props
  );
}

/**
 * A specialized hook for the TestAzureFailure BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestAzureFailure();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestAzureFailure({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestAzureFailure<Action extends typeof ServerActions.TestAzureFailureAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestAzureFailure<Action extends typeof ServerActions.TestAzureFailureAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestAzureFailure<Action extends typeof ServerActions.TestAzureFailureAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestAzureFailureStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestAzureFailureAction,
    props
  );
}

/**
 * A specialized hook for the TestCaching BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 * - not_cached: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestCaching();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestCaching({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *       not_cached: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *       not_cached: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *       not_cached: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestCaching<Action extends typeof ServerActions.TestCachingAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestCaching<Action extends typeof ServerActions.TestCachingAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestCaching<Action extends typeof ServerActions.TestCachingAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestCachingStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestCachingAction,
    props
  );
}

/**
 * A specialized hook for the TestFallbackClient BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFallbackClient();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFallbackClient({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *     }),
 *     mutate({
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFallbackClient<Action extends typeof ServerActions.TestFallbackClientAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFallbackClient<Action extends typeof ServerActions.TestFallbackClientAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFallbackClient<Action extends typeof ServerActions.TestFallbackClientAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFallbackClientStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFallbackClientAction,
    props
  );
}

/**
 * A specialized hook for the TestFallbackToShorthand BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFallbackToShorthand();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFallbackToShorthand({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFallbackToShorthand<Action extends typeof ServerActions.TestFallbackToShorthandAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFallbackToShorthand<Action extends typeof ServerActions.TestFallbackToShorthandAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFallbackToShorthand<Action extends typeof ServerActions.TestFallbackToShorthandAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFallbackToShorthandStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFallbackToShorthandAction,
    props
  );
}

/**
 * A specialized hook for the TestFnNamedArgsSingleBool BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myBool: types.boolean
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFnNamedArgsSingleBool();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFnNamedArgsSingleBool({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myBool: someValue as boolean,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myBool: firstValue as boolean,
 *     }),
 *     mutate({
 *       myBool: secondValue as boolean,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFnNamedArgsSingleBool<Action extends typeof ServerActions.TestFnNamedArgsSingleBoolAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFnNamedArgsSingleBool<Action extends typeof ServerActions.TestFnNamedArgsSingleBoolAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFnNamedArgsSingleBool<Action extends typeof ServerActions.TestFnNamedArgsSingleBoolAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFnNamedArgsSingleBoolStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFnNamedArgsSingleBoolAction,
    props
  );
}

/**
 * A specialized hook for the TestFnNamedArgsSingleClass BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myArg: types.NamedArgsSingleClass
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFnNamedArgsSingleClass();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFnNamedArgsSingleClass({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myArg: someValue as NamedArgsSingleClass,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myArg: firstValue as NamedArgsSingleClass,
 *     }),
 *     mutate({
 *       myArg: secondValue as NamedArgsSingleClass,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFnNamedArgsSingleClass<Action extends typeof ServerActions.TestFnNamedArgsSingleClassAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFnNamedArgsSingleClass<Action extends typeof ServerActions.TestFnNamedArgsSingleClassAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFnNamedArgsSingleClass<Action extends typeof ServerActions.TestFnNamedArgsSingleClassAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFnNamedArgsSingleClassStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFnNamedArgsSingleClassAction,
    props
  );
}

/**
 * A specialized hook for the TestFnNamedArgsSingleEnumList BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myArg: types.NamedArgsSingleEnumList[]
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFnNamedArgsSingleEnumList();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFnNamedArgsSingleEnumList({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myArg: someValue as NamedArgsSingleEnumList[],  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myArg: firstValue as NamedArgsSingleEnumList[],
 *     }),
 *     mutate({
 *       myArg: secondValue as NamedArgsSingleEnumList[],
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFnNamedArgsSingleEnumList<Action extends typeof ServerActions.TestFnNamedArgsSingleEnumListAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFnNamedArgsSingleEnumList<Action extends typeof ServerActions.TestFnNamedArgsSingleEnumListAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFnNamedArgsSingleEnumList<Action extends typeof ServerActions.TestFnNamedArgsSingleEnumListAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFnNamedArgsSingleEnumListStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFnNamedArgsSingleEnumListAction,
    props
  );
}

/**
 * A specialized hook for the TestFnNamedArgsSingleFloat BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myFloat: types.number
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFnNamedArgsSingleFloat();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFnNamedArgsSingleFloat({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myFloat: someValue as number,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myFloat: firstValue as number,
 *     }),
 *     mutate({
 *       myFloat: secondValue as number,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFnNamedArgsSingleFloat<Action extends typeof ServerActions.TestFnNamedArgsSingleFloatAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFnNamedArgsSingleFloat<Action extends typeof ServerActions.TestFnNamedArgsSingleFloatAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFnNamedArgsSingleFloat<Action extends typeof ServerActions.TestFnNamedArgsSingleFloatAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFnNamedArgsSingleFloatStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFnNamedArgsSingleFloatAction,
    props
  );
}

/**
 * A specialized hook for the TestFnNamedArgsSingleInt BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myInt: types.number
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFnNamedArgsSingleInt();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFnNamedArgsSingleInt({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myInt: someValue as number,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myInt: firstValue as number,
 *     }),
 *     mutate({
 *       myInt: secondValue as number,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFnNamedArgsSingleInt<Action extends typeof ServerActions.TestFnNamedArgsSingleIntAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFnNamedArgsSingleInt<Action extends typeof ServerActions.TestFnNamedArgsSingleIntAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFnNamedArgsSingleInt<Action extends typeof ServerActions.TestFnNamedArgsSingleIntAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFnNamedArgsSingleIntStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFnNamedArgsSingleIntAction,
    props
  );
}

/**
 * A specialized hook for the TestFnNamedArgsSingleMapStringToClass BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myMap: types.Record<string, StringToClassEntry>
 *
 *
 * Return Type:
 * - Non-streaming: Record<string, StringToClassEntry>
 * - Streaming Partial: RecursivePartialNull<Record<string, StringToClassEntry>>
 * - Streaming Final: Record<string, StringToClassEntry>
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFnNamedArgsSingleMapStringToClass();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Record<string, StringToClassEntry>
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Record<string, StringToClassEntry> | null
 *   partialData, // Type: RecursivePartialNull<Record<string, StringToClassEntry>> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFnNamedArgsSingleMapStringToClass({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myMap: someValue as Record<string, StringToClassEntry>,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myMap: firstValue as Record<string, StringToClassEntry>,
 *     }),
 *     mutate({
 *       myMap: secondValue as Record<string, StringToClassEntry>,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFnNamedArgsSingleMapStringToClass<Action extends typeof ServerActions.TestFnNamedArgsSingleMapStringToClassAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFnNamedArgsSingleMapStringToClass<Action extends typeof ServerActions.TestFnNamedArgsSingleMapStringToClassAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFnNamedArgsSingleMapStringToClass<Action extends typeof ServerActions.TestFnNamedArgsSingleMapStringToClassAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFnNamedArgsSingleMapStringToClassStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFnNamedArgsSingleMapStringToClassAction,
    props
  );
}

/**
 * A specialized hook for the TestFnNamedArgsSingleMapStringToMap BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myMap: types.Record<string, Record<string, string>>
 *
 *
 * Return Type:
 * - Non-streaming: Record<string, Record<string, string>>
 * - Streaming Partial: RecursivePartialNull<Record<string, Record<string, string>>>
 * - Streaming Final: Record<string, Record<string, string>>
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFnNamedArgsSingleMapStringToMap();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Record<string, Record<string, string>>
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Record<string, Record<string, string>> | null
 *   partialData, // Type: RecursivePartialNull<Record<string, Record<string, string>>> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFnNamedArgsSingleMapStringToMap({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myMap: someValue as Record<string, Record<string, string>>,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myMap: firstValue as Record<string, Record<string, string>>,
 *     }),
 *     mutate({
 *       myMap: secondValue as Record<string, Record<string, string>>,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFnNamedArgsSingleMapStringToMap<Action extends typeof ServerActions.TestFnNamedArgsSingleMapStringToMapAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFnNamedArgsSingleMapStringToMap<Action extends typeof ServerActions.TestFnNamedArgsSingleMapStringToMapAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFnNamedArgsSingleMapStringToMap<Action extends typeof ServerActions.TestFnNamedArgsSingleMapStringToMapAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFnNamedArgsSingleMapStringToMapStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFnNamedArgsSingleMapStringToMapAction,
    props
  );
}

/**
 * A specialized hook for the TestFnNamedArgsSingleMapStringToString BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myMap: types.Record<string, string>
 *
 *
 * Return Type:
 * - Non-streaming: Record<string, string>
 * - Streaming Partial: RecursivePartialNull<Record<string, string>>
 * - Streaming Final: Record<string, string>
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFnNamedArgsSingleMapStringToString();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to Record<string, string>
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: Record<string, string> | null
 *   partialData, // Type: RecursivePartialNull<Record<string, string>> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFnNamedArgsSingleMapStringToString({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myMap: someValue as Record<string, string>,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myMap: firstValue as Record<string, string>,
 *     }),
 *     mutate({
 *       myMap: secondValue as Record<string, string>,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFnNamedArgsSingleMapStringToString<Action extends typeof ServerActions.TestFnNamedArgsSingleMapStringToStringAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFnNamedArgsSingleMapStringToString<Action extends typeof ServerActions.TestFnNamedArgsSingleMapStringToStringAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFnNamedArgsSingleMapStringToString<Action extends typeof ServerActions.TestFnNamedArgsSingleMapStringToStringAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFnNamedArgsSingleMapStringToStringStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFnNamedArgsSingleMapStringToStringAction,
    props
  );
}

/**
 * A specialized hook for the TestFnNamedArgsSingleString BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myString: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFnNamedArgsSingleString();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFnNamedArgsSingleString({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myString: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myString: firstValue as string,
 *     }),
 *     mutate({
 *       myString: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFnNamedArgsSingleString<Action extends typeof ServerActions.TestFnNamedArgsSingleStringAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFnNamedArgsSingleString<Action extends typeof ServerActions.TestFnNamedArgsSingleStringAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFnNamedArgsSingleString<Action extends typeof ServerActions.TestFnNamedArgsSingleStringAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFnNamedArgsSingleStringStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFnNamedArgsSingleStringAction,
    props
  );
}

/**
 * A specialized hook for the TestFnNamedArgsSingleStringArray BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myStringArray: types.string[]
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFnNamedArgsSingleStringArray();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFnNamedArgsSingleStringArray({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myStringArray: someValue as string[],  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myStringArray: firstValue as string[],
 *     }),
 *     mutate({
 *       myStringArray: secondValue as string[],
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFnNamedArgsSingleStringArray<Action extends typeof ServerActions.TestFnNamedArgsSingleStringArrayAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFnNamedArgsSingleStringArray<Action extends typeof ServerActions.TestFnNamedArgsSingleStringArrayAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFnNamedArgsSingleStringArray<Action extends typeof ServerActions.TestFnNamedArgsSingleStringArrayAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFnNamedArgsSingleStringArrayStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFnNamedArgsSingleStringArrayAction,
    props
  );
}

/**
 * A specialized hook for the TestFnNamedArgsSingleStringList BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myArg: types.string[]
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestFnNamedArgsSingleStringList();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestFnNamedArgsSingleStringList({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myArg: someValue as string[],  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myArg: firstValue as string[],
 *     }),
 *     mutate({
 *       myArg: secondValue as string[],
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestFnNamedArgsSingleStringList<Action extends typeof ServerActions.TestFnNamedArgsSingleStringListAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestFnNamedArgsSingleStringList<Action extends typeof ServerActions.TestFnNamedArgsSingleStringListAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestFnNamedArgsSingleStringList<Action extends typeof ServerActions.TestFnNamedArgsSingleStringListAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestFnNamedArgsSingleStringListStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestFnNamedArgsSingleStringListAction,
    props
  );
}

/**
 * A specialized hook for the TestGemini BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestGemini();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestGemini({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestGemini<Action extends typeof ServerActions.TestGeminiAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestGemini<Action extends typeof ServerActions.TestGeminiAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestGemini<Action extends typeof ServerActions.TestGeminiAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestGeminiStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestGeminiAction,
    props
  );
}

/**
 * A specialized hook for the TestGeminiOpenAiGeneric BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestGeminiOpenAiGeneric();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestGeminiOpenAiGeneric({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *     }),
 *     mutate({
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestGeminiOpenAiGeneric<Action extends typeof ServerActions.TestGeminiOpenAiGenericAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestGeminiOpenAiGeneric<Action extends typeof ServerActions.TestGeminiOpenAiGenericAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestGeminiOpenAiGeneric<Action extends typeof ServerActions.TestGeminiOpenAiGenericAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestGeminiOpenAiGenericStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestGeminiOpenAiGenericAction,
    props
  );
}

/**
 * A specialized hook for the TestGeminiSystem BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestGeminiSystem();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestGeminiSystem({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestGeminiSystem<Action extends typeof ServerActions.TestGeminiSystemAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestGeminiSystem<Action extends typeof ServerActions.TestGeminiSystemAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestGeminiSystem<Action extends typeof ServerActions.TestGeminiSystemAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestGeminiSystemStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestGeminiSystemAction,
    props
  );
}

/**
 * A specialized hook for the TestGeminiSystemAsChat BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestGeminiSystemAsChat();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestGeminiSystemAsChat({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestGeminiSystemAsChat<Action extends typeof ServerActions.TestGeminiSystemAsChatAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestGeminiSystemAsChat<Action extends typeof ServerActions.TestGeminiSystemAsChatAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestGeminiSystemAsChat<Action extends typeof ServerActions.TestGeminiSystemAsChatAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestGeminiSystemAsChatStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestGeminiSystemAsChatAction,
    props
  );
}

/**
 * A specialized hook for the TestImageInput BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - img: types.Image
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestImageInput();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestImageInput({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       img: someValue as Image,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       img: firstValue as Image,
 *     }),
 *     mutate({
 *       img: secondValue as Image,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestImageInput<Action extends typeof ServerActions.TestImageInputAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestImageInput<Action extends typeof ServerActions.TestImageInputAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestImageInput<Action extends typeof ServerActions.TestImageInputAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestImageInputStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestImageInputAction,
    props
  );
}

/**
 * A specialized hook for the TestImageInputAnthropic BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - img: types.Image
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestImageInputAnthropic();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestImageInputAnthropic({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       img: someValue as Image,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       img: firstValue as Image,
 *     }),
 *     mutate({
 *       img: secondValue as Image,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestImageInputAnthropic<Action extends typeof ServerActions.TestImageInputAnthropicAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestImageInputAnthropic<Action extends typeof ServerActions.TestImageInputAnthropicAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestImageInputAnthropic<Action extends typeof ServerActions.TestImageInputAnthropicAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestImageInputAnthropicStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestImageInputAnthropicAction,
    props
  );
}

/**
 * A specialized hook for the TestImageListInput BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - imgs: types.Image[]
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestImageListInput();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestImageListInput({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       imgs: someValue as Image[],  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       imgs: firstValue as Image[],
 *     }),
 *     mutate({
 *       imgs: secondValue as Image[],
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestImageListInput<Action extends typeof ServerActions.TestImageListInputAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestImageListInput<Action extends typeof ServerActions.TestImageListInputAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestImageListInput<Action extends typeof ServerActions.TestImageListInputAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestImageListInputStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestImageListInputAction,
    props
  );
}

/**
 * A specialized hook for the TestMemory BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: TestMemoryOutput
 * - Streaming Partial: RecursivePartialNull<TestMemoryOutput>
 * - Streaming Final: TestMemoryOutput
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestMemory();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to TestMemoryOutput
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: TestMemoryOutput | null
 *   partialData, // Type: RecursivePartialNull<TestMemoryOutput> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestMemory({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestMemory<Action extends typeof ServerActions.TestMemoryAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestMemory<Action extends typeof ServerActions.TestMemoryAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestMemory<Action extends typeof ServerActions.TestMemoryAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestMemoryStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestMemoryAction,
    props
  );
}

/**
 * A specialized hook for the TestMulticlassNamedArgs BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myArg: types.NamedArgsSingleClass
 *
 * - myArg2: types.NamedArgsSingleClass
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestMulticlassNamedArgs();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestMulticlassNamedArgs({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myArg: someValue as NamedArgsSingleClass,  // Replace someValue with your data
 *       myArg2: someValue as NamedArgsSingleClass,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myArg: firstValue as NamedArgsSingleClass,
 *       myArg2: firstValue as NamedArgsSingleClass,
 *     }),
 *     mutate({
 *       myArg: secondValue as NamedArgsSingleClass,
 *       myArg2: secondValue as NamedArgsSingleClass,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestMulticlassNamedArgs<Action extends typeof ServerActions.TestMulticlassNamedArgsAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestMulticlassNamedArgs<Action extends typeof ServerActions.TestMulticlassNamedArgsAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestMulticlassNamedArgs<Action extends typeof ServerActions.TestMulticlassNamedArgsAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestMulticlassNamedArgsStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestMulticlassNamedArgsAction,
    props
  );
}

/**
 * A specialized hook for the TestNamedArgsLiteralBool BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myBool: types.true
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestNamedArgsLiteralBool();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestNamedArgsLiteralBool({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myBool: someValue as true,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myBool: firstValue as true,
 *     }),
 *     mutate({
 *       myBool: secondValue as true,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestNamedArgsLiteralBool<Action extends typeof ServerActions.TestNamedArgsLiteralBoolAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestNamedArgsLiteralBool<Action extends typeof ServerActions.TestNamedArgsLiteralBoolAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestNamedArgsLiteralBool<Action extends typeof ServerActions.TestNamedArgsLiteralBoolAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestNamedArgsLiteralBoolStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestNamedArgsLiteralBoolAction,
    props
  );
}

/**
 * A specialized hook for the TestNamedArgsLiteralInt BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myInt: types.1
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestNamedArgsLiteralInt();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestNamedArgsLiteralInt({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myInt: someValue as 1,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myInt: firstValue as 1,
 *     }),
 *     mutate({
 *       myInt: secondValue as 1,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestNamedArgsLiteralInt<Action extends typeof ServerActions.TestNamedArgsLiteralIntAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestNamedArgsLiteralInt<Action extends typeof ServerActions.TestNamedArgsLiteralIntAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestNamedArgsLiteralInt<Action extends typeof ServerActions.TestNamedArgsLiteralIntAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestNamedArgsLiteralIntStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestNamedArgsLiteralIntAction,
    props
  );
}

/**
 * A specialized hook for the TestNamedArgsLiteralString BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - myString: types."My String"
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestNamedArgsLiteralString();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestNamedArgsLiteralString({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       myString: someValue as "My String",  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       myString: firstValue as "My String",
 *     }),
 *     mutate({
 *       myString: secondValue as "My String",
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestNamedArgsLiteralString<Action extends typeof ServerActions.TestNamedArgsLiteralStringAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestNamedArgsLiteralString<Action extends typeof ServerActions.TestNamedArgsLiteralStringAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestNamedArgsLiteralString<Action extends typeof ServerActions.TestNamedArgsLiteralStringAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestNamedArgsLiteralStringStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestNamedArgsLiteralStringAction,
    props
  );
}

/**
 * A specialized hook for the TestOllama BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestOllama();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestOllama({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestOllama<Action extends typeof ServerActions.TestOllamaAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestOllama<Action extends typeof ServerActions.TestOllamaAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestOllama<Action extends typeof ServerActions.TestOllamaAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestOllamaStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestOllamaAction,
    props
  );
}

/**
 * A specialized hook for the TestOpenAILegacyProvider BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestOpenAILegacyProvider();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestOpenAILegacyProvider({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestOpenAILegacyProvider<Action extends typeof ServerActions.TestOpenAILegacyProviderAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestOpenAILegacyProvider<Action extends typeof ServerActions.TestOpenAILegacyProviderAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestOpenAILegacyProvider<Action extends typeof ServerActions.TestOpenAILegacyProviderAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestOpenAILegacyProviderStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestOpenAILegacyProviderAction,
    props
  );
}

/**
 * A specialized hook for the TestOpenAIShorthand BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestOpenAIShorthand();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestOpenAIShorthand({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestOpenAIShorthand<Action extends typeof ServerActions.TestOpenAIShorthandAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestOpenAIShorthand<Action extends typeof ServerActions.TestOpenAIShorthandAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestOpenAIShorthand<Action extends typeof ServerActions.TestOpenAIShorthandAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestOpenAIShorthandStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestOpenAIShorthandAction,
    props
  );
}

/**
 * A specialized hook for the TestRetryConstant BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestRetryConstant();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestRetryConstant({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *     }),
 *     mutate({
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestRetryConstant<Action extends typeof ServerActions.TestRetryConstantAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestRetryConstant<Action extends typeof ServerActions.TestRetryConstantAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestRetryConstant<Action extends typeof ServerActions.TestRetryConstantAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestRetryConstantStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestRetryConstantAction,
    props
  );
}

/**
 * A specialized hook for the TestRetryExponential BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestRetryExponential();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestRetryExponential({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *     }),
 *     mutate({
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestRetryExponential<Action extends typeof ServerActions.TestRetryExponentialAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestRetryExponential<Action extends typeof ServerActions.TestRetryExponentialAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestRetryExponential<Action extends typeof ServerActions.TestRetryExponentialAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestRetryExponentialStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestRetryExponentialAction,
    props
  );
}

/**
 * A specialized hook for the TestSingleFallbackClient BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestSingleFallbackClient();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestSingleFallbackClient({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *     }),
 *     mutate({
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestSingleFallbackClient<Action extends typeof ServerActions.TestSingleFallbackClientAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestSingleFallbackClient<Action extends typeof ServerActions.TestSingleFallbackClientAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestSingleFallbackClient<Action extends typeof ServerActions.TestSingleFallbackClientAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestSingleFallbackClientStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestSingleFallbackClientAction,
    props
  );
}

/**
 * A specialized hook for the TestUniverseQuestion BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - question: types.UniverseQuestionInput
 *
 *
 * Return Type:
 * - Non-streaming: UniverseQuestion
 * - Streaming Partial: RecursivePartialNull<UniverseQuestion>
 * - Streaming Final: UniverseQuestion
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestUniverseQuestion();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to UniverseQuestion
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: UniverseQuestion | null
 *   partialData, // Type: RecursivePartialNull<UniverseQuestion> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestUniverseQuestion({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       question: someValue as UniverseQuestionInput,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       question: firstValue as UniverseQuestionInput,
 *     }),
 *     mutate({
 *       question: secondValue as UniverseQuestionInput,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestUniverseQuestion<Action extends typeof ServerActions.TestUniverseQuestionAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestUniverseQuestion<Action extends typeof ServerActions.TestUniverseQuestionAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestUniverseQuestion<Action extends typeof ServerActions.TestUniverseQuestionAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestUniverseQuestionStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestUniverseQuestionAction,
    props
  );
}

/**
 * A specialized hook for the TestVertex BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestVertex();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestVertex({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string,
 *     }),
 *     mutate({
 *       input: secondValue as string,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestVertex<Action extends typeof ServerActions.TestVertexAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestVertex<Action extends typeof ServerActions.TestVertexAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestVertex<Action extends typeof ServerActions.TestVertexAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestVertexStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestVertexAction,
    props
  );
}

/**
 * A specialized hook for the TestVertexWithSystemInstructions BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 *
 * Return Type:
 * - Non-streaming: string
 * - Streaming Partial: RecursivePartialNull<string>
 * - Streaming Final: string
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useTestVertexWithSystemInstructions();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to string
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: string | null
 *   partialData, // Type: RecursivePartialNull<string> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useTestVertexWithSystemInstructions({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *     }),
 *     mutate({
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useTestVertexWithSystemInstructions<Action extends typeof ServerActions.TestVertexWithSystemInstructionsAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useTestVertexWithSystemInstructions<Action extends typeof ServerActions.TestVertexWithSystemInstructionsAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useTestVertexWithSystemInstructions<Action extends typeof ServerActions.TestVertexWithSystemInstructionsAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.TestVertexWithSystemInstructionsStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.TestVertexWithSystemInstructionsAction,
    props
  );
}

/**
 * A specialized hook for the UnionTest_Function BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - input: types.string | boolean
 *
 *
 * Return Type:
 * - Non-streaming: UnionTest_ReturnType
 * - Streaming Partial: RecursivePartialNull<UnionTest_ReturnType>
 * - Streaming Final: UnionTest_ReturnType
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useUnionTest_Function();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to UnionTest_ReturnType
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: UnionTest_ReturnType | null
 *   partialData, // Type: RecursivePartialNull<UnionTest_ReturnType> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useUnionTest_Function({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       input: someValue as string | boolean,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       input: firstValue as string | boolean,
 *     }),
 *     mutate({
 *       input: secondValue as string | boolean,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useUnionTest_Function<Action extends typeof ServerActions.UnionTest_FunctionAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useUnionTest_Function<Action extends typeof ServerActions.UnionTest_FunctionAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useUnionTest_Function<Action extends typeof ServerActions.UnionTest_FunctionAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.UnionTest_FunctionStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.UnionTest_FunctionAction,
    props
  );
}

/**
 * A specialized hook for the UseBlockConstraint BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - inp: types.BlockConstraintForParam
 *
 *
 * Return Type:
 * - Non-streaming: number
 * - Streaming Partial: RecursivePartialNull<number>
 * - Streaming Final: number
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useUseBlockConstraint();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to number
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: number | null
 *   partialData, // Type: RecursivePartialNull<number> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useUseBlockConstraint({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       inp: someValue as BlockConstraintForParam,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       inp: firstValue as BlockConstraintForParam,
 *     }),
 *     mutate({
 *       inp: secondValue as BlockConstraintForParam,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useUseBlockConstraint<Action extends typeof ServerActions.UseBlockConstraintAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useUseBlockConstraint<Action extends typeof ServerActions.UseBlockConstraintAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useUseBlockConstraint<Action extends typeof ServerActions.UseBlockConstraintAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.UseBlockConstraintStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.UseBlockConstraintAction,
    props
  );
}

/**
 * A specialized hook for the UseMalformedConstraints BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - a: types.MalformedConstraints2
 *
 *
 * Return Type:
 * - Non-streaming: number
 * - Streaming Partial: RecursivePartialNull<number>
 * - Streaming Final: number
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useUseMalformedConstraints();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to number
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: number | null
 *   partialData, // Type: RecursivePartialNull<number> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useUseMalformedConstraints({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       a: someValue as MalformedConstraints2,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       a: firstValue as MalformedConstraints2,
 *     }),
 *     mutate({
 *       a: secondValue as MalformedConstraints2,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useUseMalformedConstraints<Action extends typeof ServerActions.UseMalformedConstraintsAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useUseMalformedConstraints<Action extends typeof ServerActions.UseMalformedConstraintsAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useUseMalformedConstraints<Action extends typeof ServerActions.UseMalformedConstraintsAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.UseMalformedConstraintsStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.UseMalformedConstraintsAction,
    props
  );
}

/**
 * A specialized hook for the UseNestedBlockConstraint BAML function that handles both streaming and non-streaming responses.
 *
 * Input Types:
 *
 * - inp: types.NestedBlockConstraintForParam
 *
 *
 * Return Type:
 * - Non-streaming: number
 * - Streaming Partial: RecursivePartialNull<number>
 * - Streaming Final: number
 *
 * Common Usage Patterns:
 * 1. Non-streaming (Default)
 *    - Best for: Quick responses, simple UI updates
 *    - Avoid when: Response takes >5s or UI needs progressive updates
 *
 * 2. Streaming
 *    - Best for: Long-running operations, real-time UI feedback
 *    - Required when: Using features like chat interfaces or progress indicators
 *
 * Edge Cases & Gotchas:
 * 1. Error Handling
 *    - Network failures won't trigger onPartial/onFinal
 *    - Always implement onError for graceful degradation
 *    - Check error.message for specific failure reasons
 *
 * 2. Streaming Mode
 *    - partialData may be null even after updates (handle this case!)
 *    - Stream can end without final data (connection loss)
 *    - Partial results may be incomplete/invalid
 *
 * 3. State Management
 *    - data persists after completion (clear if needed)
 *    - isPending stays true until final/error
 *    - Multiple rapid calls can race (latest wins)
 *
 * @example
 * ```tsx
 * // 1. Basic Usage (Non-streaming)
 * const { data, error, isPending, mutate } = useUseNestedBlockConstraint();
 *
 * // Handle the response
 * useEffect(() => {
 *   if (data) {
 *     // Type-safe access to number
 *     console.log('Success:', data);
 *   }
 * }, [data]);
 *
 * // 2. Streaming with Progress
 * const {
 *   data,        // Type: number | null
 *   partialData, // Type: RecursivePartialNull<number> | null
 *   isPending,
 *   error,
 *   mutate
 * } = useUseNestedBlockConstraint({
 *   stream: true,
 *
 *   // Handle partial updates (may be incomplete!)
 *   onPartial: (partial) => {
 *     console.log('Partial:', partial);
 *   },
 *
 *   // Handle successful completion
 *   onFinal: (final) => {
 *     console.log('Final:', final);
 *   },
 *
 *   // Robust error handling
 *   onError: (error) => {
 *     console.error('Request failed:', error);
 *   }
 * });
 *
 * // 3. Making the Request
 * const handleSubmit = async () => {
 *   try {
 *     const result = await mutate({
 *       // Type-safe parameters:
 *       inp: someValue as NestedBlockConstraintForParam,  // Replace someValue with your data
 *     });
 *
 *     if (result) {
 *       // Success case
 *     }
 *   } catch (e) {
 *     // Handle any synchronous errors
 *   }
 * };
 *
 * // 4. Race Condition Handling
 * const handleMultipleCalls = async () => {
 *   // Only the latest call's results will be reflected in the UI
 *   const results = await Promise.all([
 *     mutate({
 *       inp: firstValue as NestedBlockConstraintForParam,
 *     }),
 *     mutate({
 *       inp: secondValue as NestedBlockConstraintForParam,
 *     })
 *   ]);
 *   // Check results[1] for the final state
 * };
 * ```
 */
export function useUseNestedBlockConstraint<Action extends typeof ServerActions.UseNestedBlockConstraintAction>(
  props: StreamingProps<Action>
): StreamingHookResult<Action>;

export function useUseNestedBlockConstraint<Action extends typeof ServerActions.UseNestedBlockConstraintAction>(
  props?: NonStreamingProps<Action>
): NonStreamingHookResult<Action>;

export function useUseNestedBlockConstraint<Action extends typeof ServerActions.UseNestedBlockConstraintAction>(
  props: HookProps<Action> = {},
): StreamingHookResult<Action> | NonStreamingHookResult<Action> {
  if (props.stream) {
    return useBamlAction(
      ServerActions.UseNestedBlockConstraintStreamingAction,
      props
    );
  }

  return useBamlAction(
    ServerActions.UseNestedBlockConstraintAction,
    props
  );
}
